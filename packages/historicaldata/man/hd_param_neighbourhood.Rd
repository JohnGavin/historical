% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_neighbourhood.R
\name{hd_param_neighbourhood}
\alias{hd_param_neighbourhood}
\title{Dense parameter-neighbourhood plateau-vs-peak verdict}
\usage{
hd_param_neighbourhood(
  param_values,
  metric_values,
  centre,
  min_retention = HD_PARAM_NEIGHBOURHOOD_MIN_RETENTION,
  max_cv = HD_PARAM_NEIGHBOURHOOD_MAX_CV,
  min_neighbours = HD_PARAM_NEIGHBOURHOOD_MIN_N
)
}
\arguments{
\item{param_values}{Numeric vector of parameter values tested (the
centre plus its neighbours), e.g. \code{16:24} for a dense integer
neighbourhood around \code{20}. Must be the same length as
\code{metric_values} and contain \code{centre} exactly once.}

\item{metric_values}{Numeric vector of the metric (e.g. annualised
Sharpe ratio) evaluated at each corresponding \code{param_values} entry.
\code{NA} values are never silently dropped -- their presence anywhere in
the neighbourhood makes the verdict \code{"indeterminate"}.}

\item{centre}{Numeric scalar, the chosen/production parameter value.
Must appear exactly once in \code{param_values}.}

\item{min_retention}{Numeric scalar in \verb{(0, 1]}. Minimum fraction of the
centre metric a neighbour must retain. Default
\link{HD_PARAM_NEIGHBOURHOOD_MIN_RETENTION} (\code{0.5}) -- see that constant's
roxygen for its derivation.}

\item{max_cv}{Numeric scalar \verb{> 0}. Maximum coefficient of variation
(\verb{sd / |mean|}) across the full neighbourhood. Default
\link{HD_PARAM_NEIGHBOURHOOD_MAX_CV} (\code{0.30}) -- see that constant's
roxygen for its derivation.}

\item{min_neighbours}{Positive integer scalar. Minimum number of
neighbours (excluding the centre) required to render a plateau/peak
verdict rather than \code{"indeterminate"}. Default
\link{HD_PARAM_NEIGHBOURHOOD_MIN_N} (\code{4L}).}
}
\value{
Named list:
\describe{
\item{verdict}{One of \code{"plateau"}, \code{"peak"}, \code{"indeterminate"}.}
\item{reason}{\code{"broadly_similar"} (plateau); \code{"floor"} or
\code{"dispersion"} (peak); \code{"too_few_neighbours"},
\code{"na_metric_values"}, or \code{"centre_not_positive"}
(indeterminate).}
\item{centre, centre_metric}{Echoed centre value and its metric.}
\item{n_neighbours}{Number of neighbours actually evaluated.}
\item{min_retention_ratio, cv}{The two computed statistics
(\code{NA_real_} when indeterminate before they could be computed).}
\item{min_retention, max_cv, min_neighbours}{Echoed thresholds.}
}
}
\description{
Implements John Bollinger's stated practice (see \verb{@references}): a
chosen parameter value is trustworthy only if a DENSE neighbourhood of
adjacent values produces "broadly similar behaviour" -- not merely if
the metric survives two sweep endpoints. This is stricter, on three
axes, than the existing \code{qa_parameter_robustness} two-point ±20\% sweep
documented in \code{backtest-robustness.md} §1: it inspects every point in a
dense neighbourhood (not just two endpoints), it uses an aggregate
dispersion bound in addition to a per-point floor (so a neighbourhood
that never breaches the floor at any single point can still fail on
being "wobbly"), and its intended consequence in the QA gate built on
top of it is a hard, cited decision rather than a bare \code{cli_warn()}.
}
\section{Three-state verdict}{

Returns exactly one of \code{"plateau"}, \code{"peak"}, or \code{"indeterminate"} --
never a value that could be mistaken for a pass when the neighbourhood
could not actually be assessed (\code{checks-must-distinguish-unknown.md}).
\code{"indeterminate"} fires before either substantive check runs, for
three distinct, individually reported reasons (see \code{reason}): too few
neighbours to be a meaningful density check, a missing (\code{NA}) metric
anywhere in the neighbourhood (a dropped point is never silently
excluded -- \code{fail-loud-not-null.md}), or a non-positive centre metric
(the retention-ratio floor is undefined when dividing by a non-positive
baseline; a strategy with a non-positive centre Sharpe should not be
asking "is this a plateau" in the first place).
}

\section{Two criteria, floor first}{

\enumerate{
\item \strong{Per-point floor.} Every neighbour's metric must retain
at least \code{min_retention} of the centre's metric
(\code{neighbour_metric / centre_metric >= min_retention}). This is the
dense-neighbourhood generalisation of the existing two-point ±20\%
sweep's \code{ratio < 0.5} fail condition -- same threshold, applied to
every point instead of two.
\item \strong{Aggregate dispersion.} The coefficient of variation of
the metric across the WHOLE neighbourhood (centre + all neighbours)
must not exceed \code{max_cv}. This catches a neighbourhood that swings
up and down without any single point crossing the floor -- a shape
the floor alone cannot see.
}
When the floor is breached, \code{reason} is \code{"floor"} regardless of the CV
outcome -- a single collapsed point is the more severe, easier-to-name
failure and takes priority for reporting. \code{"dispersion"} is reported
only when the floor holds everywhere but the aggregate CV still exceeds
\code{max_cv}.
}

\examples{
# A robust neighbourhood -- Bollinger's own passing case
hd_param_neighbourhood(
  param_values  = 16:24,
  metric_values = c(0.97, 1.00, 0.98, 1.02, 1.00, 0.99, 1.01, 0.98, 1.00),
  centre        = 20
)$verdict

# An isolated peak -- Bollinger's own failing case
hd_param_neighbourhood(
  param_values  = 16:24,
  metric_values = c(0.1, 0.1, 0.1, 0.1, 2.0, 0.1, 0.1, 0.1, 0.1),
  centre        = 20
)$verdict

}
\references{
The Algorithmic Advantage, "The honest truth about Bollinger Bands"
(Simon M interviewing John Bollinger, Sep 3 2026):
\url{https://algoadvantage.substack.com/p/the-honest-truth-about-bollinger}
}
\seealso{
Other falsification: 
\code{\link{hd_deflated_sharpe}()},
\code{\link{hd_delta_z}()},
\code{\link{hd_detection_power}()},
\code{\link{hd_drawdown_overlap}()},
\code{\link{hd_factor_null_test}()},
\code{\link{hd_hac_sharpe}()},
\code{\link{hd_hac_tstat}()},
\code{\link{hd_keff_frob}()},
\code{\link{hd_null_env_factor_null}()},
\code{\link{hd_null_env_garch11}()},
\code{\link{hd_null_env_gjr_garch}()},
\code{\link{hd_null_env_jump_diffusion}()},
\code{\link{hd_null_env_ma1}()},
\code{\link{hd_null_env_regime_vol}()},
\code{\link{hd_null_env_white_noise}()},
\code{\link{hd_null_rejection_rate}()},
\code{\link{hd_rolling_sharpe}()},
\code{\link{hd_sharpe_haircut}()},
\code{\link{hd_sharpe_stability_ratio}()},
\code{\link{hd_signal_null_rank}()},
\code{\link{hd_structural_breaks}()},
\code{\link{hd_tail_dependence}()},
\code{\link{hd_tail_keff_frob}()},
\code{\link{hd_top5pct_share}()},
\code{\link{hd_wf_correlation}()},
\code{\link{hd_zero_alpha_calibration}()}
}
\concept{falsification}
