% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure.R
\name{hd_exposure_metrics}
\alias{hd_exposure_metrics}
\title{Portfolio Exposure Metrics (\code{hd_exposure_metrics})}
\usage{
hd_exposure_metrics(w)
}
\arguments{
\item{w}{A numeric vector of portfolio weights. May contain negative
values (short positions) and may be named. Must have length >= 1, be
fully numeric, and contain no \code{NA}, \code{NaN}, or infinite values.}
}
\value{
A one-row \code{\link[tibble:tibble]{tibble::tibble()}} with columns:
\describe{
\item{\code{gross}}{\code{sum(abs(w))} — total notional exposure (long + short
legs). 1.0 for an unlevered long-only book; 2.0 for a dollar-neutral
long-short book at 100/100.}
\item{\code{net}}{\code{sum(w)} — directional exposure. 0 for a dollar-neutral
book; equals \code{gross} for a long-only book.}
\item{\code{long}}{\code{sum(w[w > 0])} — total long-leg notional.}
\item{\code{short}}{\code{abs(sum(w[w < 0]))} — total short-leg notional
(reported as a positive number).}
\item{\code{cash_borrow}}{\code{max(0, long - 1)} — cash borrowed as a fraction
of equity to fund the long leg above 1.0x capital. Zero whenever
\code{long <= 1}, which includes every dollar-neutral long-short book
regardless of its \code{gross} exposure.}
}
}
\description{
Computes five exposure summary statistics from a numeric portfolio-weight
vector \code{w}. This is a pure measurement function: it never renormalises,
rescales, or caps \code{w}. It exists because, across this project's
strategies, gross exposure ranges from 1.0x (long-only) to 2.0x
(dollar-neutral long-short) up to a 3.0x cap (managed futures), and no
target previously computed \code{sum(abs(w))} anywhere — the leaderboard's
\code{vol}, \code{cagr}, and \code{max_dd} columns were being compared across strategies
built at different exposure levels without that fact being visible.

\strong{Why \code{cash_borrow} is a separate quantity from \code{gross} and \code{net}:}
\code{gross} (\eqn{\sum |w_i|}) and \code{net} (\eqn{\sum w_i}) describe the shape of
the weight vector, but neither answers "how much cash does this portfolio
need to borrow?" A dollar-neutral book with 100\% long and 100\% short
(\code{gross = 2}, \code{net = 0}) borrows \strong{zero} cash in the conventional prime-
brokerage sense: the long leg is funded by the investor's own equity, and
the short leg is funded by the proceeds of the short sale itself (held as
collateral), not by borrowed cash. Cash borrowing only arises when the
\strong{long} leg exceeds 100\% of equity — i.e. margin debt used to buy more
long exposure than the account is worth. \code{cash_borrow} therefore measures
\code{max(0, long - 1)}, which is zero for every long-short strategy in this
project's registry (see \code{strategy_gross_convention}) and positive only for
a leveraged long-only book.
}
\details{
Gross, net, and cash-borrowing exposure of a weight vector
}
\examples{
# Long-only, unlevered (100\% in one asset)
hd_exposure_metrics(c(0.5, 0.5))

# Long-only, levered to 120\% (20\% margin debt)
hd_exposure_metrics(c(0.7, 0.5))

# Dollar-neutral long-short at 2.0x gross (100 long / 100 short) --
# cash_borrow is 0: the short leg is proceeds-funded, not cash-borrowed.
hd_exposure_metrics(c(0.5, 0.5, -0.5, -0.5))

}
\seealso{
Other risk_metrics: 
\code{\link{hd_cdap}()},
\code{\link{hd_dd_duration}()},
\code{\link{hd_first_passage}()},
\code{\link{hd_loss_clustering}()},
\code{\link{hd_risk_contribution}()}
}
\concept{risk_metrics}
