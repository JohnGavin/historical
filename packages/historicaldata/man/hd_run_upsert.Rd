% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_writers.R
\name{hd_run_upsert}
\alias{hd_run_upsert}
\title{Idempotently upsert a backtest run}
\usage{
hd_run_upsert(
  con,
  strategy_id,
  started_at = Sys.time(),
  finished_at = Sys.time(),
  status = "success",
  git_sha = NULL,
  git_dirty = FALSE,
  pipeline_version = NA_character_,
  partition = NA_character_,
  universe_id = NA_character_,
  cost_model_id = NA_character_,
  parent_uuid = NA_character_,
  notes = NA_character_
)
}
\arguments{
\item{con}{DBI connection (writable).}

\item{strategy_id}{The strategy this run belongs to. Must already exist
in \code{bt.strategy} (FK enforced).}

\item{started_at, finished_at}{POSIXct timestamps. Defaults: now / now.}

\item{status}{Character. \code{"success"}, \code{"failed"}, \code{"partial"}. Default
\code{"success"}.}

\item{git_sha}{Character. Defaults to \code{Sys.getenv("HD_GIT_SHA")} or
\verb{git rev-parse HEAD} if the env var is unset and \code{git} is on PATH.}

\item{git_dirty}{Logical. Defaults to \code{FALSE} if not provided.}

\item{pipeline_version}{Character. Optional pipeline tag. Defaults to NA.}

\item{partition}{Character. Optional sub-config tag. Defaults to NA.}

\item{universe_id, cost_model_id}{FK refs. Default NA.}

\item{parent_uuid}{Optional parent run for nested / dependency runs.}

\item{notes}{Optional free text.}
}
\value{
Character — the \code{run_uuid} (existing or newly minted UUID v4).
}
\description{
Like \code{\link[=hd_run_record]{hd_run_record()}}, but safe to call repeatedly with the same
\verb{(strategy_id, partition, pipeline_version)} tuple. On a re-run:
}
\details{
\enumerate{
\item Looks up the existing \code{run_uuid} in \code{bt.run}.
\item Deletes any \code{bt.metric} rows for that UUID (the caller re-inserts
fresh metrics immediately after).
\item Updates \code{finished_at}, \code{duration_sec}, \code{status}, and \code{notes} on the
existing run row.
\item Returns the existing \code{run_uuid} — callers that subsequently call
\code{\link[=hd_metric_record]{hd_metric_record()}} with this UUID will write updated metrics.
}

If no existing row matches, falls back to \code{\link[=hd_run_record]{hd_run_record()}} (insert path).

This function fixes the deterministic-RNG UUID collision when \code{targets}
calls \verb{set.seed(<target_hash>)} before a sentinel target, causing
\code{\link[=.new_uuid]{.new_uuid()}} to return the same UUID on every re-run (#375).
}
