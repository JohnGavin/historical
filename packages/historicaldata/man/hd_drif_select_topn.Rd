% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drif_selection.R
\name{hd_drif_select_topn}
\alias{hd_drif_select_topn}
\title{Select top-N DRIF factors via rank-first, filter-benchmark-second}
\usage{
hd_drif_select_topn(predictions, params, top_n)
}
\arguments{
\item{predictions}{A tibble with at least the columns:
\itemize{
\item \code{factor_name} character — ticker / factor identifier
\item \code{ym} character — year-month key (\code{"YYYY-MM"})
\item \code{pred_rank} integer — pre-computed rank within each \code{ym} group
(lower = higher predicted return).  If absent, the function ranks
on \code{predicted_ret} (production column name) or \code{predicted}
(multiverse column name).
\item One of \code{predicted_ret} or \code{predicted} — the raw prediction value,
used to compute \code{pred_rank} when it is missing.
}}

\item{params}{A list with at least:
\itemize{
\item \code{factors} character vector — investable factor names (benchmark excluded)
}}

\item{top_n}{Integer scalar — number of top-ranked factors to select.}
}
\value{
A tibble of the same column structure as \code{predictions}, filtered to
the top-\code{top_n} factors (benchmark excluded) per month.  Returns a
zero-row tibble (same columns as input) if no rows survive.
}
\description{
Shared selection logic used by both the production DRIF pipeline
(\code{plan_drif.R}) and the multiverse runner (\code{plan_drif_v2.R}).
}
\details{
The order of operations is critical:
\enumerate{
\item Rank \strong{all} predicted series (including benchmark) within each date
group so that the benchmark competes for rank.
\item Filter to the investable factor pool (\code{params$factors}), dropping the
benchmark ticker.
\item Retain only rows where \code{pred_rank <= top_n}.
}

Doing the filter before the rank (the old multiverse code path) produced
different decile membership when the benchmark ranked highly, making the
multiverse's "current spec" non-comparable to production.
}
