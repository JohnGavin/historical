% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/min_var_weights.R
\name{hd_min_var_weights}
\alias{hd_min_var_weights}
\title{Global Minimum-Variance Weights (\code{hd_min_var_weights})}
\usage{
hd_min_var_weights(Sigma, normalize = TRUE)
}
\arguments{
\item{Sigma}{A \eqn{p \times p} symmetric, positive-definite numeric matrix.
Must have \code{p >= 2}. Column names (or row names) are used to name the
returned weight vector.}

\item{normalize}{Logical scalar. If \code{TRUE} (default), weights are
normalised so they sum to exactly 1. If \code{FALSE}, the raw
\code{solve(Sigma, rep(1, p))} solution is returned (useful for diagnostics).}
}
\value{
A named numeric vector of length \code{p}. When \code{normalize = TRUE}, the
weights sum to 1. Names are taken from \code{dimnames(Sigma)[[1]]} (row names
of the covariance matrix); \code{NULL} if \code{Sigma} has no dimnames.
}
\description{
Returns the unconstrained global minimum-variance (GMV) portfolio weights for
a given covariance matrix \code{Sigma}. The GMV solution is the unique vector of
portfolio weights that minimises portfolio variance subject only to the
budget constraint (\eqn{\mathbf{1}^\top w = 1}):

\deqn{w^* = \frac{\Sigma^{-1} \mathbf{1}}{\mathbf{1}^\top \Sigma^{-1} \mathbf{1}}}

Implemented via \code{solve(Sigma, rep(1, p))} followed by normalisation, which
is numerically equivalent and avoids explicit matrix inversion.

\strong{Singularity:} the sample covariance matrix is singular when the number of
assets \eqn{p} exceeds the number of observations \eqn{n}. When \code{Sigma} is
singular or numerically ill-conditioned, \code{solve()} will fail and
\code{hd_min_var_weights()} aborts with an informative error suggesting the caller
use a regularised estimator (see \code{\link[=hd_cov_estimate]{hd_cov_estimate()}}). This failure is the
\emph{intended} demonstration that plain sample covariance breaks in the wide
(\eqn{p \geq n}) regime — the diagnostic function \code{\link[=hd_cov_oos_diagnostic]{hd_cov_oos_diagnostic()}}
counts these failures per method.
}
\details{
Global minimum-variance portfolio weights (unconstrained, closed form)
}
\examples{
# 3-asset example with a known covariance matrix
Sigma <- matrix(c(0.04, 0.01, 0.00,
                  0.01, 0.09, 0.02,
                  0.00, 0.02, 0.16),
                nrow = 3, ncol = 3,
                dimnames = list(c("A","B","C"), c("A","B","C")))

w <- hd_min_var_weights(Sigma)
sum(w)       # 1
names(w)     # "A" "B" "C"

# GMV variance is lower than equal-weight variance
p <- nrow(Sigma)
w_eq <- rep(1/p, p)
var_gmv <- as.numeric(t(w) \%*\% Sigma \%*\% w)
var_eq  <- as.numeric(t(w_eq) \%*\% Sigma \%*\% w_eq)
var_gmv <= var_eq  # TRUE
}
\references{
Markowitz, H. (1952). Portfolio selection. \emph{Journal of Finance}, 7(1),
77–91. \doi{10.2307/2975974}

Merton, R. C. (1972). An analytic derivation of the efficient portfolio
frontier. \emph{Journal of Financial and Quantitative Analysis}, 7(4),
1851–1872. \doi{10.2307/2329621}
}
\seealso{
Other covariance: 
\code{\link{hd_cov_estimate}()},
\code{\link{hd_cov_oos_diagnostic}()},
\code{\link{hd_min_var_weights_penalised}()},
\code{\link{hd_weight_stability_diagnostic}()}
}
\concept{covariance}
