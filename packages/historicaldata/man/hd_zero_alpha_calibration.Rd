% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zero_alpha_calibration.R
\name{hd_zero_alpha_calibration}
\alias{hd_zero_alpha_calibration}
\title{Zero-alpha calibration surface for composite-book Sharpe inflation}
\usage{
hd_zero_alpha_calibration(
  grid,
  T_obs = 60L,
  n_reps = 200L,
  rho_bar = 0,
  sigma_annual = 0.2,
  ann_factor = 12,
  seed = 42L
)
}
\arguments{
\item{grid}{A data frame with integer columns \code{n_trials} and \code{n_legs}.
\code{n_legs} must satisfy \verb{1 <= n_legs <= n_trials} in every row (aborts
otherwise, naming the offending row indices).}

\item{T_obs}{Integer. Length of each candidate return series. Default
\code{60L} (5 years of monthly data).}

\item{n_reps}{Integer. Monte Carlo repetitions per grid point. Default
\code{200L}.}

\item{rho_bar}{Numeric in \verb{[0, 1)}. Target pairwise correlation among
the \code{n_trials} candidates within a repetition -- see
\code{.hd_null_panel_correlated()}. Default \code{0} (independent candidates).
This is a SUPPLIED parameter, not fitted from data; correlation-
structure estimation against the real strategy grammar is explicitly
out of scope for this function (#839).}

\item{sigma_annual}{Numeric. Annualised volatility of each candidate.
Default \code{0.20}.}

\item{ann_factor}{Integer. Annualisation factor for the Sharpe
computation (e.g. \code{12} for monthly, \code{252} for daily). Default \code{12}.}

\item{seed}{Integer. Base random seed; every grid point / repetition
uses a deterministic seed derived from this, so the whole surface is
reproducible. Default \code{42L}.}
}
\value{
A tibble, one row per grid point, with columns \code{n_trials},
\code{n_legs}, \code{rho_bar}, \code{n_reps}, \code{mean_sharpe}, \code{median_sharpe},
\code{sd_sharpe}, \code{q05_sharpe}, \code{q95_sharpe} -- the manufactured-Sharpe
distribution's summary statistics at that operating point, computed
entirely on data with zero true alpha by construction.
}
\description{
Estimates how much of a reported in-sample Sharpe ratio is
\emph{manufactured} by the combination of (a) selecting the best-looking
result out of many zero-alpha trials (multiple testing -- the axis
\code{hd_strat_keff_vertox()} / \code{hd_deflated_sharpe()} already correct for)
and (b) equal-weight blending several selected results into one
reported "book" (variance reduction on correlated legs -- an axis
neither of those functions targets). See the file header (\verb{R/zero_ alpha_calibration.R}) for the motivating citation and #839.
}
\details{
At each \verb{(n_trials, n_legs)} grid point, this function repeatedly: (1)
draws \code{n_trials} zero-alpha (no true signal, by construction) candidate
return series from \code{.hd_null_panel_correlated()} with target pairwise
correlation \code{rho_bar}; (2) ranks them by in-sample Sharpe and keeps the
top \code{n_legs}; (3) equal-weights the selected legs into one book; (4)
computes the book's in-sample Sharpe. The result is the \emph{distribution},
across \code{n_reps} repetitions, of the Sharpe ratio a genuinely-zero-alpha
pipeline would report at that operating point.
}
\section{Manufactured-Sharpe estimate, NOT an out-of-sample forecast}{

The Sharpe values this function returns describe how much apparent edge
the trial-count + leg-blending mechanism \emph{manufactures on its own}, on
data with no true signal. They are a haircut estimate for a reported
in-sample Sharpe -- NOT a prediction of what any real strategy will earn
going forward, and regime effects or genuine structural change can
dominate this correction in either direction (the article's own
section 5 finding). In particular:
\itemize{
\item Do NOT use a shrinking gap between a reported Sharpe and its
calibrated-null Sharpe as grounds to revise or abandon a strategy --
see \code{.claude/rules/resulting-prohibition.md} (judge process, not
outcome).
\item Do NOT treat multi-year underperformance following a strategy's
launch as evidence this calibration was "wrong" -- see
\code{.claude/rules/underperformance-prior.md} (historically normal
drawdown durations are long, sometimes decades).
}
This function calibrates a search/aggregation artefact. It is not a
return forecast, and it must never be used as one.
}

\seealso{
\code{\link[=hd_detection_power]{hd_detection_power()}} for the sibling first-class-metric
precedent (#726); \code{\link[=hd_strat_keff_vertox]{hd_strat_keff_vertox()}} and \code{\link[=hd_deflated_sharpe]{hd_deflated_sharpe()}}
for the trial-count multiple-testing correction this function
complements (it targets the leg-blending axis, which those do not).

Other falsification: 
\code{\link{hd_deflated_sharpe}()},
\code{\link{hd_delta_z}()},
\code{\link{hd_detection_power}()},
\code{\link{hd_drawdown_overlap}()},
\code{\link{hd_factor_null_test}()},
\code{\link{hd_hac_sharpe}()},
\code{\link{hd_hac_tstat}()},
\code{\link{hd_keff_frob}()},
\code{\link{hd_null_env_factor_null}()},
\code{\link{hd_null_env_garch11}()},
\code{\link{hd_null_env_gjr_garch}()},
\code{\link{hd_null_env_jump_diffusion}()},
\code{\link{hd_null_env_ma1}()},
\code{\link{hd_null_env_regime_vol}()},
\code{\link{hd_null_env_white_noise}()},
\code{\link{hd_null_rejection_rate}()},
\code{\link{hd_rolling_sharpe}()},
\code{\link{hd_sharpe_haircut}()},
\code{\link{hd_sharpe_stability_ratio}()},
\code{\link{hd_signal_null_rank}()},
\code{\link{hd_structural_breaks}()},
\code{\link{hd_tail_dependence}()},
\code{\link{hd_tail_keff_frob}()},
\code{\link{hd_top5pct_share}()},
\code{\link{hd_wf_correlation}()}
}
\concept{falsification}
