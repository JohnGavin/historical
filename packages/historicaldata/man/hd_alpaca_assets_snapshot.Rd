% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpaca.R
\name{hd_alpaca_assets_snapshot}
\alias{hd_alpaca_assets_snapshot}
\title{Snapshot the Alpaca assets universe to parquet}
\usage{
hd_alpaca_assets_snapshot(
  out_dir,
  asset_class = NULL,
  status = c("active", "inactive", "both"),
  date = Sys.Date(),
  paper = TRUE,
  key = NULL,
  secret = NULL,
  min_rows = 100L
)
}
\arguments{
\item{out_dir}{Directory for the parquet. Created if missing.}

\item{asset_class}{One of \code{"us_equity"}, \code{"crypto"}, or \code{NULL} for all.}

\item{status}{One of \code{"active"}, \code{"inactive"}, or \code{"both"}. Default \code{"active"}.}

\item{date}{Snapshot date (defaults to today); embedded in the filename.}

\item{paper}{If \code{TRUE} (default), hits \code{paper-api.alpaca.markets}; else
\code{api.alpaca.markets}. Assets metadata is identical between tiers.}

\item{key, secret}{Optional explicit credentials. If \code{NULL} (default),
reads from \code{ALPACA_KEY} / \code{ALPACA_SECRET} env vars.}

\item{min_rows}{Soft floor — warn if the response held fewer rows than this.}
}
\value{
Path to the written parquet (invisibly).
}
\description{
Calls the Alpaca Markets \code{v2/assets} endpoint and writes the response as
parquet. Captures the 18 fields documented in the Alpaca API: identifiers
(\code{id}, \code{class}, \code{exchange}, \code{symbol}, \code{name}), tradability flags (\code{status},
\code{tradable}, \code{marginable}, \code{shortable}, \code{easy_to_borrow}, \code{fractionable}),
margin requirements, attributes (e.g. \code{has_options}, \code{etp},
\code{ptp_no_exception}), and order-sizing constraints (\code{min_order_size},
\code{min_trade_increment}, \code{price_increment}).
}
\details{
This is a thin direct-API wrapper — the upstream \code{{alpacar}} package
(datawookie/alpacar) is GitHub-only and therefore not in our nix pin, so we
call \code{httr2} directly. Functionally equivalent to \code{alpacar::assets_list()}
for the 18 documented fields.
}
\section{Credentials}{

Reads keys from environment variables:
\itemize{
\item \code{ALPACA_KEY} — Alpaca API key ID
\item \code{ALPACA_SECRET} — Alpaca API secret
}
Add these to \verb{~/.Renviron} (NOT to project files). The Alpaca canonical
SDK variable names are \code{APCA_API_KEY_ID} / \code{APCA_API_SECRET_KEY}; if you
use those instead, pass them explicitly via \code{key} / \code{secret}. Aborts early
if either is missing — does not silently fall through.
}

\section{Account type}{

Paper-trading and live accounts return identical assets metadata (same
universe, same fields, same conids). Default \code{paper = TRUE} is correct
for all metadata-only work; switch only if you genuinely need the
live-data tier.
}

\section{Survivorship}{

Pass \code{status = "inactive"} (or \code{"both"}) to retrieve delisted symbols.
The default \code{"active"} gives the currently-tradeable universe only — same
survivorship trap as a yfinance snapshot. Pair with periodic snapshots
to build a point-in-time universe (see issue #150).
}

\seealso{
Other discovery: 
\code{\link{hd_datasets}()},
\code{\link{hd_ecb_registry}()},
\code{\link{hd_exchanges}()},
\code{\link{hd_fred_meta}()},
\code{\link{hd_jst_variables}()},
\code{\link{hd_macro_forward}()},
\code{\link{hd_macro_registry}()},
\code{\link{hd_macro_series}()},
\code{\link{hd_search}()},
\code{\link{hd_summary}()},
\code{\link{hd_ticker_info}()},
\code{\link{hd_ticker_meta}()},
\code{\link{hd_tickers}()},
\code{\link{hd_yahoo_screen_snapshot}()}
}
\concept{discovery}
