% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_metrics.R
\name{hd_record_stability_metrics}
\alias{hd_record_stability_metrics}
\title{Record SSR and top-pct-share stability metrics for a backtest run}
\usage{
hd_record_stability_metrics(
  con,
  run_uuid,
  returns,
  w,
  ann_factor = 252,
  pct = 0.05
)
}
\arguments{
\item{con}{DBI connection (writable).}

\item{run_uuid}{Character. Must refer to an existing \code{bt.run} row.}

\item{returns}{Numeric vector of period returns.}

\item{w}{Integer window length for the rolling Sharpe computation.
Typical choices: 252 (daily) or 36 (monthly).}

\item{ann_factor}{Annualisation factor matching the frequency of \code{returns}
(252 for daily, 12 for monthly, 4 for quarterly). Default 252.}

\item{pct}{Numeric in (0, 1). The top fraction for \code{\link[=hd_top5pct_share]{hd_top5pct_share()}}.
Default 0.05 (top 5\%).}
}
\value{
Invisibly, a named list of the 8 metric values.
}
\description{
Convenience wrapper that calls \code{\link[=hd_sharpe_stability_ratio]{hd_sharpe_stability_ratio()}} and
\code{\link[=hd_top5pct_share]{hd_top5pct_share()}} on a returns vector, then writes the combined
8 metric values into \code{bt.metric} via \code{\link[=hd_metric_record]{hd_metric_record()}}.
}
\details{
The 8 metrics written are (unit in parens; see \code{\link[=hd_metric_units]{hd_metric_units()}}):
\describe{
\item{\code{ssr}}{Sharpe Stability Ratio. (\code{ratio})}
\item{\code{ssr_mean_sharpe}}{Mean of rolling Sharpe series. (\code{ratio})}
\item{\code{ssr_se}}{Newey-West HAC standard error. (\code{ratio})}
\item{\code{ssr_n_windows}}{Number of complete rolling windows. (\code{count})}
\item{\code{ssr_lag_nw}}{Newey-West bandwidth used. (\code{count})}
\item{\code{top_share}}{Fraction of total return from the top-\code{pct} periods. (\code{fraction})}
\item{\code{top_n_top}}{Count of top-\code{pct} periods. (\code{count})}
\item{\code{top_n_total}}{Total non-NA period count. (\code{count})}
}
These units are fixed by this helper's own contract — always the same
regardless of caller — so they are hardcoded here rather than accepted
as an argument.

\code{NA} values in \code{returns} are silently removed before both computations
(matching the behaviour of \code{\link[=hd_sharpe_stability_ratio]{hd_sharpe_stability_ratio()}} and
\code{\link[=hd_top5pct_share]{hd_top5pct_share()}}).  If all values are \code{NA} the function returns
a list of eight \code{NA} values without writing anything to the database.

Idempotency: \code{\link[=hd_metric_record]{hd_metric_record()}} performs a DELETE-then-INSERT for each
\verb{(run_uuid, metric_name)} pair, so calling this helper twice with the same
arguments overwrites the earlier values and leaves exactly 8 rows.
}
\seealso{
\code{\link[=hd_sharpe_stability_ratio]{hd_sharpe_stability_ratio()}}, \code{\link[=hd_top5pct_share]{hd_top5pct_share()}},
\code{\link[=hd_metric_record]{hd_metric_record()}}
}
