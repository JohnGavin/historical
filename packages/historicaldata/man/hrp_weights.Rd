% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topological_risk_parity.R
\name{hrp_weights}
\alias{hrp_weights}
\title{Hierarchical Risk Parity (HRP) portfolio weights}
\usage{
hrp_weights(cov_mat, method = "complete")
}
\arguments{
\item{cov_mat}{Numeric matrix. Named, symmetric, positive-semi-definite
covariance matrix (n x n). Row and column names must be the same and
identify the assets.}

\item{method}{Character scalar. Linkage method passed to \link[stats:hclust]{stats::hclust}.
Default \code{"complete"} is most common for HRP. Other valid values:
\code{"single"}, \code{"average"}, \code{"ward.D2"}.}
}
\value{
Named numeric vector of portfolio weights summing to 1. All weights
are strictly positive (inverse-variance allocation assigns positive weight
to every asset).
}
\description{
Implements the Lopez de Prado (2016) HRP algorithm using only base-R
clustering (\code{stats::hclust}, \code{stats::dist}). No external optimiser or
portfolio package is required.
}
\details{
The three steps are:
\enumerate{
\item \strong{Tree clustering} — hierarchical agglomerative clustering on the
correlation-distance matrix (complete linkage by default).
\item \strong{Quasi-diagonalisation} — reorder assets by dendrogram leaf order so
correlated assets are adjacent.
\item \strong{Recursive bisection} — split portfolio allocation top-down using the
inverse-variance of each sub-cluster.
}
}
\examples{
set.seed(42)
n <- 5
R <- matrix(c(1,.8,.1,.1,.0,
              .8,1,.1,.1,.0,
              .1,.1,1,.7,.2,
              .1,.1,.7,1,.2,
              .0,.0,.2,.2,1), nrow = n)
sds <- c(.02,.03,.015,.025,.02)
cov_mat <- diag(sds) \%*\% R \%*\% diag(sds)
rownames(cov_mat) <- colnames(cov_mat) <- paste0("A", seq_len(n))
w <- hrp_weights(cov_mat)
stopifnot(abs(sum(w) - 1) < 1e-10, all(w > 0))
}
\references{
Lopez de Prado, M. (2016). Building diversified portfolios that outperform
out-of-sample. \emph{Journal of Portfolio Management}, 42(4), 59–69.
}
\seealso{
Other topological_risk_parity: 
\code{\link{trp_weights}()}
}
\concept{topological_risk_parity}
