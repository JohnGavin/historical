% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alphavantage.R
\name{hd_alphavantage}
\alias{hd_alphavantage}
\title{Fetch daily adjusted OHLCV from AlphaVantage}
\usage{
hd_alphavantage(
  ticker,
  from = NULL,
  outputsize = c("compact", "full"),
  key = NULL
)
}
\arguments{
\item{ticker}{A single ticker symbol (e.g. \code{"AAPL"}, \code{"IBM"}).}

\item{from}{Start date (character \code{"YYYY-MM-DD"} or \code{Date}). Default: 20
years ago. AlphaVantage returns ~20 years on the full output size.}

\item{outputsize}{One of \code{"compact"} (last 100 data points, default) or
\code{"full"} (up to 20 years). Use \code{"full"} for historical backtests.}

\item{key}{API key. If \code{NULL} (default), reads \code{ALPHAVANTAGE_API_KEY} env var.}
}
\value{
A tibble with columns: \code{date}, \code{open}, \code{high}, \code{low}, \code{close},
\code{adjusted_close}, \code{volume}, \code{ticker}.
}
\description{
Calls AlphaVantage \code{TIME_SERIES_DAILY_ADJUSTED} and returns a tidy tibble
with the standard \code{historicaldata} schema. Respects the free-tier rate limit
of 5 calls/minute via \code{Sys.sleep()} between batch calls.
}
\section{Credentials}{

API key is read from \code{Sys.getenv("ALPHAVANTAGE_API_KEY")}. Set in
\verb{~/.Renviron} (NOT in project code). The function aborts early with an
informative message if the key is absent.
}

\section{Rate limits}{

The AlphaVantage free tier allows 5 requests / minute and 500 / day.
For batches larger than 5 tickers, add a \code{Sys.sleep(12)} between each
call or use \code{hd_av_batch()} (not yet implemented) which handles throttling
automatically.
}

\section{CI safety}{

This function makes a live API call. Do NOT call from CI on every push.
Gate on \code{AV_REFRESH=1} environment variable or use a scheduled workflow.
}

\examples{
\dontshow{if (interactive() && nzchar(Sys.getenv("ALPHAVANTAGE_API_KEY"))) withAutoprint(\{ # examplesIf}
hd_alphavantage("IBM", from = "2024-01-01")
\dontshow{\}) # examplesIf}
}
\seealso{
Other data-access: 
\code{\link{hd_check_survivorship_bias}()},
\code{\link{hd_ecb}()},
\code{\link{hd_factors}()},
\code{\link{hd_guardian}()},
\code{\link{hd_guardian_monthly}()},
\code{\link{hd_jst}()},
\code{\link{hd_lazy}()},
\code{\link{hd_macro}()},
\code{\link{hd_macro_series}()},
\code{\link{hd_macro_vintages}()},
\code{\link{hd_ohlcv}()}
}
\concept{data-access}
