% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hd_strat_keff_vertox.R
\name{hd_strat_keff_vertox}
\alias{hd_strat_keff_vertox}
\title{Effective number of tested strategies (Vertox K_eff_strat)}
\usage{
hd_strat_keff_vertox(sigma, n_sim = 20000L, seed = NULL, tol = 0.001)
}
\arguments{
\item{sigma}{Strategy-return correlation matrix (\eqn{M \times M},
symmetric, positive semi-definite, unit diagonal). Typically computed
from a leaderboard's strategy return-series.}

\item{n_sim}{Monte Carlo sample size for E\link{max Z² | Σ}. Default 20,000 —
enough that the MC standard error on \code{K_eff_strat} is below 0.05 for
typical leaderboards. Raise for tighter convergence; lower for speed.}

\item{seed}{Optional integer for reproducibility. \code{NULL} leaves the
global RNG state alone.}

\item{tol}{Numerical tolerance for the Brent root-finder. Default 1e-3
(i.e. \code{K_eff_strat} accurate to ~0.001 of a strategy).}
}
\value{
Scalar \code{K_eff_strat} in \verb{[1, M]}. Never \code{NA}; boundary cases
return exact \code{1} or \code{M} (see Details).
}
\description{
Computes the correlation-aware effective count of tested strategies
\code{K_eff_strat}, as defined in
\href{https://www.vertoxquant.com/p/the-effective-number-of-tested-strategies}{VertoxQuant (2026)}.
This is the foundation for any correct multiple-testing correction
(Deflated Sharpe, Bonferroni, FDR, Harvey/Liu) applied to a \emph{portfolio of
correlated strategies}. See issue #160 for the audit + 4-PR plan.
}
\details{
Defined implicitly via:
\deqn{E[\max_i Z_i^2 \mid \Sigma] = E[\max_i Z_i^2 \mid I_{K}]}
where the left side is evaluated under the actual strategy-return
correlation \eqn{\Sigma} (size \eqn{M}), the right side under
\eqn{K} independent standard normals, and \code{K_eff_strat} is the
\eqn{K} that makes the two equal.

Boundary behaviour (axioms from the article):
\itemize{
\item \eqn{\Sigma = I_M} → \code{K_eff_strat = M} (no correlation, no shrinkage)
\item \eqn{\Sigma = \mathbf{1}\mathbf{1}^\top} → \code{K_eff_strat = 1} (perfect correlation collapses to 1)
\item Adding a new strategy weakly increases \code{K_eff_strat} (monotonicity)
}

This implementation uses Monte Carlo simulation for the correlated side
(cheap and trivially correct for any PSD \eqn{\Sigma}) and analytic
integration for the iid side. Stochasticity is controlled via the \code{seed}
argument for reproducible pipeline targets.
}
\section{Companion quantity}{

This is distinct from \code{K_eff_acf} (autocorrelation-adjusted effective
sample size \emph{per series}, Newey-West) computed by
\code{\link[=calculate_keff]{calculate_keff()}} in \code{R/tail_keff.R}. Bare \code{K_eff} (no suffix) is
reserved — always use the suffixed name.
}

\references{
VertoxQuant (2026), \emph{The Effective Number of Tested
Strategies}. \url{https://www.vertoxquant.com/p/the-effective-number-of-tested-strategies}
}
\concept{multiple-testing}
