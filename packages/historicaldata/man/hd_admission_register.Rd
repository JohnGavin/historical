% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_admission.R
\name{hd_admission_register}
\alias{hd_admission_register}
\title{Pre-register a strategy in the admission table}
\usage{
hd_admission_register(
  con,
  strategy,
  hypothesis,
  expected = list(),
  reviewer,
  gate_result = NULL,
  override = FALSE,
  override_reason = NA_character_
)
}
\arguments{
\item{con}{A writable DBI connection from \code{\link[=hd_registry_open]{hd_registry_open()}} with
\code{read_only = FALSE}.}

\item{strategy}{Character. Unique strategy identifier (matches
\code{bt.strategy.strategy_id} convention but is not enforced as a FK here
— Phase 2 will add the FK).}

\item{hypothesis}{Character. Free-text hypothesis / rationale for
admission. Required.}

\item{expected}{A named list of pre-registered expectations:
\describe{
\item{incr_sharpe}{Numeric. Expected incremental Sharpe contribution.}
\item{var_reduction}{Numeric. Expected annualised variance reduction.}
\item{target_regime}{Character. Market regime the strategy is designed
for (e.g. \code{"trending"}, \code{"mean-reverting"}, \code{"all-weather"}).}
\item{max_corr}{Numeric. Maximum expected correlation with existing
strategies.}
}
Missing list elements are stored as \code{NULL}.}

\item{reviewer}{Character. Name or GitHub handle of the reviewer.}

\item{gate_result}{Optional tibble returned by \code{\link[=hd_strategy_value_gate]{hd_strategy_value_gate()}}.
If supplied, \code{gate_overall} is extracted from \code{attr(gate_result, "overall")}
and the full tibble is serialised to \code{gate_detail_json} via
\code{\link[jsonlite:fromJSON]{jsonlite::toJSON()}}.}

\item{override}{Logical. Set \code{TRUE} if admitting despite a \code{"reject"}
gate result (requires \code{override_reason}). Default \code{FALSE}.}

\item{override_reason}{Character. Required when \code{override = TRUE}. Default
\code{NA_character_}.}
}
\value{
Invisibly returns the \code{admission_uuid} (character).
}
\description{
Inserts or updates one row in \code{strategy_admission} keyed on \code{strategy}.
On re-registration of an existing strategy, \code{admission_uuid} and
\code{admitted_at} are \strong{preserved} from the original row and all other
fields are updated. This lets callers revise expectations without losing
the original admission timestamp.
}
\details{
If the table does not exist yet, \code{\link[=hd_admission_init]{hd_admission_init()}} is called
automatically.
}
\examples{
\dontrun{
tf  <- tempfile(fileext = ".duckdb")
con <- DBI::dbConnect(duckdb::duckdb(), dbdir = tf)
withr::defer(DBI::dbDisconnect(con, shutdown = TRUE))
hd_admission_init(con)
uuid <- hd_admission_register(
  con       = con,
  strategy  = "strat_demo",
  hypothesis = "Trend-following in EM equities adds diversification",
  expected  = list(incr_sharpe = 0.10, var_reduction = 0.002,
                   target_regime = "trending", max_corr = 0.35),
  reviewer  = "john"
)
uuid
}
}
\seealso{
Other governance: 
\code{\link{hd_admission_init}()},
\code{\link{hd_admission_read}()},
\code{\link{hd_strategy_value_gate}()}
}
\concept{governance}
