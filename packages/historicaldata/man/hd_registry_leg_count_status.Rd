% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_metrics.R
\name{hd_registry_leg_count_status}
\alias{hd_registry_leg_count_status}
\title{Leg-count / manufactured-Sharpe calibration coverage from the registry}
\usage{
hd_registry_leg_count_status(con)
}
\arguments{
\item{con}{DBI connection (read-only is fine).}
}
\value{
A tibble with columns \code{strategy_id}, \code{leg_count},
\code{has_leg_calibration} (logical) -- one row per composite strategy
(\code{leg_count > 1}) currently in \code{bt.strategy}. Zero rows if no
composite strategy is registered yet.
}
\description{
For every strategy in \code{bt.strategy} whose \code{leg_count > 1} (a declared
composite/blended book -- see \code{\link[=hd_strategy_upsert]{hd_strategy_upsert()}}'s \code{leg_count}
section, #839), reports whether ANY of its runs has an accompanying
manufactured-Sharpe calibration annotation, recorded via
\code{\link[=hd_diagnostic_record]{hd_diagnostic_record()}} under \code{diagnostic_name = "leg_blend_manufactured_sharpe"} -- the diagnostic name
\code{\link[=hd_zero_alpha_calibration]{hd_zero_alpha_calibration()}}'s output is intended to feed.
}
\details{
This is a pure read -- it does not itself decide pass/fail; see
\code{check_registry_leg_count_calibration()} (\code{R/plan_qa_gates.R}, QA gate
S32) for the gate built on top of this reader.
}
