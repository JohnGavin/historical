% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_paths.R
\name{hd_simulate_paths}
\alias{hd_simulate_paths}
\title{Simulate joint multi-asset annual return paths}
\usage{
hd_simulate_paths(
  n_paths,
  horizon_years,
  assets,
  Sigma = NULL,
  mu = NULL,
  method = c("parametric", "bootstrap"),
  cpi_annual_rate = 0.03,
  block_size = 12L,
  .returns_wide = NULL,
  .cpi_monthly = NULL,
  seed = 42L
)
}
\arguments{
\item{n_paths}{Positive integer. Number of simulation paths.}

\item{horizon_years}{Positive integer. Years to project forward.}

\item{assets}{Character vector of asset tickers.}

\item{Sigma}{Numeric matrix (n_assets x n_assets). Annualised
covariance. If NULL, estimated from \code{.returns_wide}.}

\item{mu}{Named numeric vector of annualised expected returns.
If NULL, estimated as 12 x colMeans of \code{.returns_wide}.}

\item{method}{One of \code{"parametric"} (MASS::mvrnorm) or
\code{"bootstrap"} (block-bootstrap from \code{.returns_wide}).}

\item{cpi_annual_rate}{Numeric scalar. Constant annual CPI deflator
(default 0.03). Phase E will replace with bootstrapped CPI draws.}

\item{block_size}{Integer. Monthly block length for bootstrap
(default 12L — one year, preserves seasonal structure).}

\item{.returns_wide}{Data frame with columns \code{date} + one column
per asset (monthly returns). Required for \code{method = "bootstrap"}
and for estimating mu/Sigma when either is NULL.}

\item{.cpi_monthly}{Optional numeric vector of historical monthly CPI
changes (month-over-month, e.g. from FRED CPIAUCSL). When supplied and
\code{length(.cpi_monthly) >= block_size}, annual CPI rates are drawn by
block-bootstrapping 12 consecutive monthly values (same \code{block_size} and
\code{seed} as the return draws). When NULL (default), \code{cpi_annual_rate} is
used as a constant. Phase E of issue #389.}

\item{seed}{Integer random seed (default 42L).}
}
\value{
A tibble with 7 columns:
\describe{
\item{path_id}{Integer (1..n_paths)}
\item{year}{Integer (1..horizon_years)}
\item{asset}{Character ticker}
\item{return_nominal}{Nominal annual return}
\item{return_real}{Real return: (1+nominal)/(1+cpi) - 1}
\item{cum_nominal}{Cumulative nominal growth factor from t=0}
\item{cum_real}{Cumulative real growth factor from t=0}
}
}
\description{
Generates Monte Carlo paths for a set of assets using either a
parametric (multivariate-normal) or block-bootstrap approach.
Phase C of issue #389 — multivariate return simulation.
}
