% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cov_estimate.R
\name{hd_cov_estimate}
\alias{hd_cov_estimate}
\title{Regularised Covariance Estimator (\code{hd_cov_estimate})}
\usage{
hd_cov_estimate(
  returns,
  method = c("sample", "ledoit_wolf", "rmt_denoise", "threshold"),
  lw_target = c("const_cor", "identity"),
  threshold = 0.1,
  threshold_type = c("soft", "hard"),
  assume_centered = FALSE
)
}
\arguments{
\item{returns}{A numeric matrix or data frame with rows = observations
(n) and columns = assets (p). Data frames are coerced to a numeric
matrix; a \code{date} column (any column whose name equals \code{"date"},
case-insensitively) is dropped first. Column names are preserved as
\code{dimnames} on the output.}

\item{method}{Estimator to use. One of \code{"sample"}, \code{"ledoit_wolf"},
\code{"rmt_denoise"}, \code{"threshold"}. Partial matching is supported via
\code{\link[base:match.arg]{base::match.arg()}}.}

\item{lw_target}{Structured target for Ledoit-Wolf shrinkage. One of
\code{"const_cor"} (constant-correlation target, Ledoit & Wolf 2004 "Honey,
I Shrunk…") or \code{"identity"} (scaled-identity target). Ignored unless
\code{method = "ledoit_wolf"}.}

\item{threshold}{Off-diagonal correlation threshold for \code{method = "threshold"}. Must be in [0, 1). Default \code{0.1}.}

\item{threshold_type}{Type of thresholding. One of \code{"soft"} or \code{"hard"}.
Ignored unless \code{method = "threshold"}.}

\item{assume_centered}{Logical. If \code{TRUE}, the data are treated as
already demeaned and the column means are not subtracted before
estimation. Default \code{FALSE}.}
}
\value{
A symmetric p × p numeric matrix with the same \code{dimnames} as the
columns of \code{returns}. The following attributes are always set:
\describe{
\item{\code{method}}{Character scalar — the estimator used.}
\item{\code{n_obs}}{Integer — number of complete-case rows used.}
\item{\code{n_assets}}{Integer — number of assets (columns) p.}
\item{\code{condition_number}}{Numeric — ratio of the largest to the
smallest eigenvalue (a measure of numerical ill-conditioning).}
}
Additional attributes for specific methods:
\describe{
\item{\code{shrinkage}}{(\code{ledoit_wolf} only) Optimal shrinkage intensity
δ* in [0, 1].}
\item{\code{n_clipped}}{(\code{rmt_denoise} only) Number of eigenvalues below
the Marchenko-Pastur upper edge that were clipped to their mean.}
}
}
\description{
Computes a regularised covariance matrix from a returns matrix or
data frame using one of four methods suited to the \emph{p >> n} (wide-data)
regime common in portfolio construction:
\itemize{
\item \strong{\code{"sample"}} — classical n−1 sample covariance (baseline; singular
when p > n).
\item \strong{\code{"ledoit_wolf"}} — linear shrinkage toward a structured target
(Ledoit & Wolf 2004). Guaranteed positive-definite for any n, p.
\item \strong{\code{"rmt_denoise"}} — Marchenko-Pastur eigenvalue clipping. Noise
eigenvalues (below the MP upper edge) are collapsed to their mean so
the trace is preserved, then the matrix is reconstructed.
\item \strong{\code{"threshold"}} — soft or hard thresholding of off-diagonal
correlations. Simple but can break positive-definiteness; a warning is
issued when that occurs.
}

Missing values are handled by dropping rows with any \code{NA} (complete
cases). A \code{date} column, if present in a data frame input, is silently
dropped before estimation.
}
\details{
Regularised covariance estimator for wide financial data
}
\examples{
set.seed(42)
X <- matrix(rnorm(60 * 10), nrow = 60, ncol = 10)
colnames(X) <- paste0("A", seq_len(10))

# baseline — singular when p > n
S_sample <- hd_cov_estimate(X, method = "sample")

# Ledoit-Wolf shrinkage (constant-correlation target)
S_lw <- hd_cov_estimate(X, method = "ledoit_wolf", lw_target = "const_cor")
attr(S_lw, "shrinkage")   # optimal delta*

# RMT denoising
S_rmt <- hd_cov_estimate(X, method = "rmt_denoise")
attr(S_rmt, "n_clipped")  # how many eigenvalues were noise

# hard thresholding at 0.2
S_thr <- hd_cov_estimate(X, method = "threshold", threshold = 0.2,
                         threshold_type = "hard")
}
\references{
Ledoit, O. & Wolf, M. (2004). A well-conditioned estimator for
large-dimensional covariance matrices. \emph{Journal of Multivariate
Analysis}, 88(2), 365–411. \doi{10.1016/S0047-259X(03)00096-4}

Ledoit, O. & Wolf, M. (2004). Honey, I Shrunk the Sample Covariance
Matrix. \emph{Journal of Portfolio Management}, 30(4), 110–119.
\doi{10.3905/jpm.2004.110}

Laloux, L., Cizeau, P., Bouchaud, J.-P. & Potters, M. (1999). Noise
Dressing of Financial Correlation Matrices. \emph{Physical Review Letters},
83(7), 1467–1470. \doi{10.1103/PhysRevLett.83.1467}

Raviv, E. (2026). Covariance Estimation for Wide Data. \emph{WIREs
Computational Statistics}, 18(2). \doi{10.1002/wics.70068}
}
\seealso{
Other covariance: 
\code{\link{hd_cov_oos_diagnostic}()},
\code{\link{hd_min_var_weights}()}
}
\concept{covariance}
