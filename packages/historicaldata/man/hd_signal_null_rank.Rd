% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/falsification.R
\name{hd_signal_null_rank}
\alias{hd_signal_null_rank}
\title{Rank a strategy's real metric within a distribution of signal-null metrics}
\usage{
hd_signal_null_rank(actual_metric, null_metrics, dominance_threshold = 0.5)
}
\arguments{
\item{actual_metric}{Numeric scalar. The real strategy's metric (e.g.
annualised Sharpe) computed with its real, uncorrupted signal.}

\item{null_metrics}{Numeric vector. The same metric computed on each of
N signal-corrupted variants. May contain \code{NA} (e.g. a variant with too
few observations to compute a Sharpe); these are excluded before
ranking. Length 1 is a valid degenerate case (a single corrupted
replicate, as the pre-#718 mom_prepeak test used).}

\item{dominance_threshold}{Numeric scalar in \verb{[0, 1]}. Fraction of valid
null variants that must perform at least as well as \code{actual_metric} for
\code{null_dominates} to be \code{TRUE}. Default \code{0.5} (a majority).}
}
\value{
Named list:
\describe{
\item{actual_metric}{The input \code{actual_metric}, unchanged.}
\item{n_beat}{Count of valid null variants with metric \verb{>= actual_metric}.
\code{NA_integer_} when indeterminate (see below).}
\item{n_valid}{Count of null variants with a non-\code{NA} metric.}
\item{n_total}{\code{length(null_metrics)}, including any \code{NA}s.}
\item{rank_pct}{\code{n_beat / n_valid}. \code{NA_real_} when indeterminate.}
\item{null_dominates}{Logical: is \code{rank_pct >= dominance_threshold}?
\code{NA} -- never coerced to \code{FALSE} -- when \code{actual_metric} is \code{NA} or
\code{n_valid == 0}, per \code{checks-must-distinguish-unknown}: an
unmeasurable verdict must not look like a passing one.}
}
}
\description{
Generic statistical layer for signal-null falsification tests (#718,
generalising the strategy-specific placebo test that previously existed
only for \code{mom_prepeak} -- see \code{.mom_prepeak_random_peak_signal()}
in \code{R/plan_mom_prepeak_gauntlet.R}, the reference implementation).
}
\details{
A DATA null (\code{hd_null_env_*} in this file) asks whether performance is
distinguishable from the same strategy run on a synthetic series with no
edge. A SIGNAL null asks a narrower, complementary question: on REAL data,
holding every other piece of machinery fixed (costs, sizing, execution),
is performance distinguishable from the same machinery with the claimed
edge-carrying signal corrupted or randomised? A strategy can pass every
data null while harvesting a structural property of real markets rather
than its stated signal -- the signal null is designed to catch that case.

This function performs only the comparison: given the real strategy's
metric and a vector of the same metric computed on N signal-corrupted
variants, it returns a rank statistic ("k of N corrupted variants beat the
real strategy"), mirroring Heger (2026)'s "0 of 80" construction without
any distributional assumption. HOW to corrupt a given strategy's signal is
declared per strategy -- see \code{.mom_prepeak_random_peak_signal()}
(mom_prepeak), \code{olmar_backtest(signal_null = TRUE)} (OLMAR-1,
cross-sectional shuffle of the predicted price relative), and
\code{.mf_signal_null_signs()} (managed futures, random trend sign) --
and is intentionally NOT automated here.
}
\seealso{
Other falsification: 
\code{\link{hd_deflated_sharpe}()},
\code{\link{hd_delta_z}()},
\code{\link{hd_detection_power}()},
\code{\link{hd_drawdown_overlap}()},
\code{\link{hd_factor_null_test}()},
\code{\link{hd_hac_sharpe}()},
\code{\link{hd_hac_tstat}()},
\code{\link{hd_keff_frob}()},
\code{\link{hd_null_env_factor_null}()},
\code{\link{hd_null_env_garch11}()},
\code{\link{hd_null_env_gjr_garch}()},
\code{\link{hd_null_env_jump_diffusion}()},
\code{\link{hd_null_env_ma1}()},
\code{\link{hd_null_env_regime_vol}()},
\code{\link{hd_null_env_white_noise}()},
\code{\link{hd_null_rejection_rate}()},
\code{\link{hd_rolling_sharpe}()},
\code{\link{hd_sharpe_stability_ratio}()},
\code{\link{hd_structural_breaks}()},
\code{\link{hd_tail_dependence}()},
\code{\link{hd_tail_keff_frob}()},
\code{\link{hd_top5pct_share}()},
\code{\link{hd_wf_correlation}()}
}
\concept{falsification}
