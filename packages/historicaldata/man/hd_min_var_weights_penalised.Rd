% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/min_var_penalised.R
\name{hd_min_var_weights_penalised}
\alias{hd_min_var_weights_penalised}
\title{Penalised Global Minimum-Variance Weights (\code{hd_min_var_weights_penalised})}
\usage{
hd_min_var_weights_penalised(
  Sigma,
  lambda_ridge = 0,
  w_prev = NULL,
  lambda_turnover = 0,
  normalize = TRUE
)
}
\arguments{
\item{Sigma}{A \eqn{p \times p} symmetric, positive-definite numeric matrix.
Must have \code{p >= 2}. Row names are used to name the returned weight vector.}

\item{lambda_ridge}{Non-negative scalar. Regularisation intensity for the
ridge (L2) covariance penalty. \code{0} (default) means no ridge regularisation.
Larger values shrink weights toward the equal-risk portfolio and improve
the conditioning of the penalised matrix.}

\item{w_prev}{Named or unnamed numeric vector of length \code{p} — the prior
portfolio weights toward which the L2 turnover penalty pulls the solution.
Required when \code{lambda_turnover > 0}; ignored (and must be \code{NULL}) when
\code{lambda_turnover == 0}. A warning is issued if \code{w_prev} does not sum to
approximately 1 (tolerance 0.01).}

\item{lambda_turnover}{Non-negative scalar. Intensity of the L2 turnover
penalty. \code{0} (default) disables the penalty. Requires \code{w_prev} when > 0.}

\item{normalize}{Logical scalar. If \code{TRUE} (default), the returned weights
are normalised to sum exactly to 1. If \code{FALSE}, the raw solution of the
linear system is returned (useful for diagnostics).}
}
\value{
A named numeric vector of length \code{p}. When \code{normalize = TRUE} the
weights sum to 1. Names are taken from \code{dimnames(Sigma)[[1]]}; \code{NULL} if
\code{Sigma} has no dimnames. The following attributes are always attached:
\describe{
\item{\code{lambda_ridge}}{The ridge penalty used.}
\item{\code{lambda_turnover}}{The turnover penalty used.}
\item{\code{condition_number}}{The 2-norm condition number of the penalised
matrix \eqn{A = \Sigma + (\lambda_r + \lambda_t) I}, computed via
\code{\link[base:kappa]{base::kappa()}}. Lower values indicate better conditioning.}
\item{\code{turnover}}{Sum of absolute weight changes
\eqn{\sum |w^* - w_{\text{prev}}|}; set only when \code{w_prev} is
supplied.}
}
}
\description{
Returns penalised global minimum-variance (GMV) portfolio weights via a
\strong{single linear solve} — no quadratic-programming solver required.
Two closed-form penalties are supported simultaneously:
\itemize{
\item \strong{Ridge (L2 on covariance):} adds \eqn{\lambda_{\text{ridge}} I} to the
covariance matrix before inversion, shrinking extreme weights toward an
equal-risk allocation and improving numerical conditioning.
\item \strong{L2 turnover penalty toward prior weights:} minimises
\deqn{w^\top (\Sigma + \lambda_r I + \lambda_t I) w - 2\lambda_t w_{\text{prev}}^\top w}
subject to \eqn{\mathbf{1}^\top w = 1}. The KKT/Lagrange solution is:
\deqn{w^* = \lambda_t A^{-1} w_{\text{prev}} +
              \frac{1 - \lambda_t \mathbf{1}^\top A^{-1} w_{\text{prev}}}
                   {\mathbf{1}^\top A^{-1} \mathbf{1}} \, A^{-1} \mathbf{1}}
where \eqn{A = \Sigma + (\lambda_r + \lambda_t) I}. Both inverses are
computed in a single \code{solve()} call.
}

When both penalties are zero the function reduces exactly to
\code{\link[=hd_min_var_weights]{hd_min_var_weights()}}.

\strong{Look-ahead-bias note:} penalty intensities (\code{lambda_ridge},
\code{lambda_turnover}) and the prior weights \code{w_prev} must be determined from
in-window/training data only. Using hold-out data to calibrate these
parameters introduces look-ahead bias into the optimised weights.

\strong{Deferred (Phase 3b — requires a QP solver/new dependency):}
no-short constraints (\eqn{w \geq 0}), box constraints, L1 turnover
penalty, and robust/worst-case MVO. These will be implemented in a
follow-up PR once \code{quadprog} (or similar) is added to DESCRIPTION and
the Nix environment is regenerated.
}
\details{
Penalised global minimum-variance portfolio weights (closed form)
}
\examples{
# 3-asset well-conditioned covariance matrix
Sigma <- matrix(c(0.04, 0.01, 0.00,
                  0.01, 0.09, 0.02,
                  0.00, 0.02, 0.16),
                nrow = 3, ncol = 3,
                dimnames = list(c("A","B","C"), c("A","B","C")))

# Plain GMV — identical to hd_min_var_weights(Sigma) when penalties are 0
w0 <- hd_min_var_weights_penalised(Sigma)
sum(w0)   # 1

# Ridge-penalised GMV — better conditioning, moderate regularisation
w_ridge <- hd_min_var_weights_penalised(Sigma, lambda_ridge = 0.01)
attr(w_ridge, "condition_number")  # lower than attr(w0, "condition_number")

# Turnover-damped GMV — pull weights toward a prior allocation
w_prev <- c(A = 0.5, B = 0.3, C = 0.2)
w_to   <- hd_min_var_weights_penalised(Sigma, w_prev = w_prev,
                                        lambda_turnover = 0.1)
attr(w_to, "turnover")  # sum(|w_to - w_prev|)
}
\references{
Markowitz, H. (1952). Portfolio selection. \emph{Journal of Finance}, 7(1),
77–91. \doi{10.2307/2975974}

DeMiguel, V., Garlappi, L., & Uppal, R. (2009). Optimal versus naive
diversification: How inefficient is the 1/N portfolio strategy? \emph{Review of
Financial Studies}, 22(5), 1915–1953. \doi{10.1093/rfs/hhm075}

Ledoit, O. & Wolf, M. (2004). A well-conditioned estimator for
large-dimensional covariance matrices. \emph{Journal of Multivariate Analysis},
88(2), 365–411. \doi{10.1016/S0047-259X(03)00096-4}
}
\seealso{
Other covariance: 
\code{\link{hd_cov_estimate}()},
\code{\link{hd_cov_oos_diagnostic}()},
\code{\link{hd_min_var_weights}()},
\code{\link{hd_weight_stability_diagnostic}()}
}
\concept{covariance}
