% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_artefacts.R
\name{check_artefact_registry}
\alias{check_artefact_registry}
\title{Validate artefact registry against disk}
\usage{
check_artefact_registry(con, docs_dir, strict = TRUE)
}
\arguments{
\item{con}{DBI connection (read-only is fine).}

\item{docs_dir}{Directory where rendered HTML lives (typically
\code{here::here("docs")}).}

\item{strict}{Logical. If \code{TRUE} (default), missing artefacts cause an
\code{rlang::abort()} via \code{cli::cli_abort}. If \code{FALSE}, return the
missing-issues tibble without aborting.}
}
\value{
Invisibly, a tibble of missing artefacts:
\code{tibble(vignette_id, html_path, abs_path)}. Empty when all paths
resolve.
}
\description{
Reads every row from \code{art.vignette} and verifies that the file at
\code{html_path} (interpreted relative to \code{docs_dir}) actually exists.
Vignettes with \code{html_path = NA} or \code{status = 'draft'} are skipped.
}
\details{
This is the gate that would have caught the \code{mermaid-test.html} +
\code{examples.html} regressions in 2026-05: registry rows survived even
after the files were removed by a render-cleanup pass.
}
