% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hd_markov_transition.R
\name{hd_markov_transition}
\alias{hd_markov_transition}
\title{Estimate a first-order Markov transition matrix from a discrete state series}
\usage{
hd_markov_transition(state, states = NULL)
}
\arguments{
\item{state}{A character or factor vector of state labels, ordered in
time (\code{state[t]} immediately precedes \code{state[t + 1]}). Must contain at
least 2 distinct non-\code{NA} values.}

\item{states}{\code{NULL} (default), or a character vector declaring the
allowed vocabulary. When \code{state} is a factor, \code{levels(state)} is used
as the declared vocabulary UNLESS \code{states} is supplied explicitly, in
which case \code{states} wins. When \code{state} is a plain character vector and
\code{states} is \code{NULL}, the vocabulary is inferred from the observed
non-\code{NA} values. Any observed value not in the declared vocabulary
aborts -- this function never silently coerces an unrecognised state to
\code{NA} or drops it.}
}
\value{
Named list:
\describe{
\item{transition_matrix}{\code{n_states} x \code{n_states} numeric matrix, rows
sum to 1 (row/column named by state). Row \code{i}, column \code{j} is
\verb{P(state_t+1 = j | state_t = i)}. A row is entirely \code{NA} when that
state was never observed as an origin (\code{state_t}) -- see
\code{persistence$n_from}.}
\item{counts}{Same shape, raw transition counts (integer matrix).}
\item{persistence}{Tibble with columns \code{state}, \code{p_stay}, \code{n_from} --
the diagonal of \code{transition_matrix}, one row per declared state, in
declared order.}
\item{states}{Character vector, the declared vocabulary in order.}
\item{n_transitions}{Integer, the number of valid (non-\code{NA} origin
AND destination) one-step transitions counted.}
\item{n_dropped}{Integer, the number of adjacent pairs excluded
because at least one side was \code{NA}.}
}
}
\description{
Counts observed one-step transitions in \code{state} (in time order) and
returns the transition-probability matrix \verb{P(state_t+1 = j | state_t = i)} plus the diagonal persistence probabilities \verb{P(stay in state i)}. Per
\code{.claude/rules/fail-loud-not-null.md}, an unrecognised or insufficient
state series aborts loudly rather than silently returning \code{NA} or
dropping rows without disclosure.
}
\examples{
# Sticky 2-state series: state persists most of the time
s <- c("low", "low", "low", "high", "high", "low", "low", "low")
out <- hd_markov_transition(s)
out$persistence

}
\seealso{
Other regime-diagnostics: 
\code{\link{hd_stationarity_diagnostics}()}
}
\concept{regime-diagnostics}
