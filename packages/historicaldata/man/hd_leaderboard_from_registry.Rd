% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_metrics.R
\name{hd_leaderboard_from_registry}
\alias{hd_leaderboard_from_registry}
\title{Leaderboard read from the registry}
\usage{
hd_leaderboard_from_registry(
  con,
  metric_name = "sharpe",
  latest_per_partition = TRUE
)
}
\arguments{
\item{con}{DBI connection (read-only is fine).}

\item{metric_name}{Character. Default \code{"sharpe"}.}

\item{latest_per_partition}{Logical. If \code{TRUE} (default), keep only
the most recent run per (strategy_id, partition). If \code{FALSE}, every
recorded run is returned.}
}
\value{
A tibble.
}
\description{
Joins \code{bt.strategy} + \code{bt.run} + \code{bt.metric} and pivots the named
metric (default \code{"sharpe"}) into a column. Returns one row per
(strategy_id, partition, run_uuid).
}
