% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy_mom_prepeak.R
\name{hd_mom_prepeak_signal}
\alias{hd_mom_prepeak_signal}
\title{Pre-peak / post-peak 12-2 momentum decomposition (Büsing et al. 2022)}
\usage{
hd_mom_prepeak_signal(
  daily_prices,
  as_of_dates,
  lookback_months_start = 12L,
  lookback_months_end = 2L,
  min_obs_days = 100L
)
}
\arguments{
\item{daily_prices}{Tibble with columns \code{ticker} (character), \code{date}
(Date), \code{adjusted} (numeric, adjusted close).}

\item{as_of_dates}{Date vector. One row per (ticker, as_of_date) is
produced for tickers with sufficient history.}

\item{lookback_months_start}{Integer. Start of formation window expressed
as months before \code{as_of_date}. Default 12L (the "12" in 12-2).}

\item{lookback_months_end}{Integer. End of formation window expressed as
months before \code{as_of_date}. Default 2L (the "2" in 12-2 — skips the
most recent month to avoid short-term reversal).}

\item{min_obs_days}{Integer. Minimum trading days required in the formation
window for a row to be returned. Default 100L. Rows below this threshold
are dropped (NOT returned as NA — they have no decomposition to report).}
}
\value{
Tibble with one row per (ticker, as_of_date) that had >= \code{min_obs_days}
observations in the formation window. Columns:
\itemize{
\item \code{ticker} — character
\item \code{as_of_date} — Date
\item \code{formation_start} — Date (first date in window)
\item \code{formation_end}   — Date (last date in window)
\item \code{peak_date}       — Date (date of max adjusted price)
\item \code{n_obs}           — integer (trading days in window)
\item \code{pre_peak_return}  — numeric (price(peak) / price(formation_start) - 1)
\item \code{post_peak_return} — numeric (price(formation_end) / price(peak) - 1)
\item \code{total_return}    — numeric (price(formation_end) / price(formation_start) - 1)
\item \code{peak_position}   — numeric in \link{0, 1} (peak's relative position in window;
0 = peak on day 1, 1 = peak on last day) — useful for replicating
paper Figure 1 (peak-date distribution histogram)
}
}
\description{
For each (ticker, as_of_date), find the single date within the formation
window \link{as_of_date - lookback_months_start, as_of_date - lookback_months_end}
on which the stock's adjusted price was highest, then decompose the
formation-window return into pre-peak (start -> peak) and post-peak
(peak -> formation-end) components.
}
\details{
Look-ahead safety: the formation window is strictly before as_of_date.
No price at or after as_of_date enters the computation.

Sanity identity (verified in tests): up to compounding,
\code{(1 + pre_peak_return) * (1 + post_peak_return) == (1 + total_return)}.

Edge cases:
\itemize{
\item Peak on formation-start (day 1) -> \code{pre_peak_return == 0},
\code{post_peak_return == total_return}.
\item Peak on formation-end (last day) -> \code{pre_peak_return == total_return},
\code{post_peak_return == 0}.
\item Ties for peak: the FIRST occurrence is used (deterministic via
\code{which.max()} semantics).
}
}
\concept{momentum_prepeak}
