% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fundamentals.R
\name{hd_fundamentals}
\alias{hd_fundamentals}
\title{Query point-in-time equity fundamentals (revision triangle)}
\usage{
hd_fundamentals(
  ticker = NULL,
  xbrl_tag = NULL,
  as_of = NULL,
  strict_same_day = FALSE,
  include_latest = FALSE,
  local = FALSE,
  collect = TRUE
)
}
\arguments{
\item{ticker}{Character vector. \code{NULL} (default) returns all tickers.}

\item{xbrl_tag}{Character vector of XBRL tags (e.g. \code{"Revenues"},
\code{"EarningsPerShareDiluted"}). \code{NULL} (default) returns all tags.}

\item{as_of}{Date (or coercible). When supplied, only rows with
\code{first_filed <= as_of} (or \code{first_filed < as_of} when
\code{strict_same_day = TRUE}) are returned -- the point-in-time cutoff.
\code{NULL} (default) applies no cutoff and returns the full history --
this is NOT point-in-time safe; see the \code{look-ahead-bias-prevention}
rule.}

\item{strict_same_day}{If \code{TRUE}, use \code{first_filed < as_of} (a filing on
the same day as \code{as_of} is NOT considered visible yet). Default
\code{FALSE} treats a filing as visible on its own filing date (inclusive).}

\item{include_latest}{If \code{TRUE}, also return \code{latest_value} and
\code{restated} (post-restatement values, screening-only -- see Details).
Default \code{FALSE} returns only the as-first-reported \code{original_value}.}

\item{local}{If \code{TRUE}, resolve to the local cache path instead of the
registry's remote URL. See \code{\link[=hd_dataset_source]{hd_dataset_source()}}.}

\item{collect}{If \code{TRUE} (default), materialise. If \code{FALSE}, return a
lazy duckplyr frame.}
}
\value{
Tibble (or lazy frame) with columns \code{ticker}, \code{fiscal_period},
\code{period_end}, \code{first_filed}, \code{xbrl_tag}, \code{original_value}, \code{source},
plus \code{latest_value} and \code{restated} when \code{include_latest = TRUE}.
}
\description{
Returns fundamental/accounting data (XBRL line items from SEC EDGAR
filings) as it was known at a given \code{as_of} date, protecting backtests
from the filing-lag and restatement leaks documented in GitHub issue
\href{https://github.com/JohnGavin/historical/issues/553}{#553}.
}
\details{
The underlying \code{fundamentals} dataset stores ONE ROW per
\verb{(ticker, xbrl_tag, fiscal_period)}, carrying both the value AS FIRST
REPORTED (\code{original_value}) and the value AFTER any later restatement
(\code{latest_value}), keyed by \code{first_filed} -- the date the filing became
public. This mirrors the pattern used for macro data
(\code{\link[=hd_macro_vintages]{hd_macro_vintages()}}) but condenses the full filing history to
first/latest rather than storing every intermediate revision as its own
row (a deliberate scope simplification -- see the #553 schema decision).

By default this function returns only \code{original_value} and enforces
\code{first_filed <= as_of} when \code{as_of} is supplied -- exactly what a
backtest must use to avoid peeking at numbers the market could not yet
see. Returning \code{latest_value} is an explicit opt-in
(\code{include_latest = TRUE}) for CURRENT screening use only: it is NOT
point-in-time safe (the restated filing can post-date \code{as_of}) and must
never be used inside a backtest.

\strong{Coverage (as of the #553/#554/#555 pilot):} a fixed set of large-cap
US tickers and 4 XBRL tags, fetched by
\code{scripts/fetch_fundamentals_edgar.R}. This is NOT a broad-universe
dataset -- see \code{\link[=hd_datasets]{hd_datasets()}}\verb{$fundamentals$description} for the exact
pilot scope. A ticker not in the pilot list returns zero rows, not an
error.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
hd_fundamentals("AAPL", as_of = "2020-01-31")
hd_fundamentals("AAPL", xbrl_tag = "EarningsPerShareDiluted", as_of = Sys.Date())
hd_fundamentals("AAPL", include_latest = TRUE)  # screening only, NOT PIT-safe
\dontshow{\}) # examplesIf}
}
\seealso{
Other data-access: 
\code{\link{hd_alphavantage}()},
\code{\link{hd_check_survivorship_bias}()},
\code{\link{hd_ecb}()},
\code{\link{hd_factors}()},
\code{\link{hd_guardian}()},
\code{\link{hd_guardian_monthly}()},
\code{\link{hd_jst}()},
\code{\link{hd_kraken_ohlcvt}()},
\code{\link{hd_lazy}()},
\code{\link{hd_macro}()},
\code{\link{hd_macro_series}()},
\code{\link{hd_macro_vintages}()},
\code{\link{hd_ohlcv}()}
}
\concept{data-access}
