% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admission_preregister.R
\name{hd_admission_preregister}
\alias{hd_admission_preregister}
\title{Pre-register a strategy-admission hypothesis in the research log}
\usage{
hd_admission_preregister(
  strategy,
  hypothesis,
  expected = list(),
  reviewer,
  counterparty = NA_character_,
  kill_criterion = NA_character_,
  expected_sharpe = NA_real_,
  ann_factor = 12,
  n_tests = 1,
  correction = c("none", "bonferroni"),
  target_power = 0.8,
  gate_result = NULL,
  revises = FALSE,
  base_dir = NULL,
  seal = TRUE
)
}
\arguments{
\item{strategy}{Character. Unique strategy identifier (matches
\code{strategy_names$code_name} convention).}

\item{hypothesis}{Character. Free-text rationale for admission (evidence,
not outcome -- see \code{.claude/rules/resulting-prohibition.md}).}

\item{expected}{A named list of pre-registered expectations:
\describe{
\item{incr_sharpe}{Numeric. Expected incremental Sharpe contribution.}
\item{var_reduction}{Numeric. Expected annualised variance reduction.}
\item{target_regime}{Character. Market regime the strategy is designed
for (e.g. \code{"trending"}, \code{"mean-reverting"}, \code{"all-weather"}).}
\item{max_corr}{Numeric. Maximum expected correlation with existing
strategies.}
}
Missing list elements are stored as \code{NA}.}

\item{reviewer}{Character. Name or GitHub handle of the reviewer. Required.}

\item{counterparty}{Character. Who is expected to be on the other side of
this trade, and why they are willing to give up the edge (#902 row 5 --
"If I can't name the mechanism and the counterparty, I don't have an
edge"). Default \code{NA_character_}.}

\item{kill_criterion}{Character. A machine-checkable condition under which
this strategy should be withdrawn, stated BEFORE admission (#902 row 9).
Default \code{NA_character_}.}

\item{expected_sharpe}{Numeric scalar > 0, or \code{NA_real_} (default). The
Sharpe ratio this strategy is expected to deliver. When supplied,
\code{\link[=hd_detection_power]{hd_detection_power()}} is called to compute the sample (in years) that
would be needed to detect it, recorded alongside the expectation --
\code{.claude/rules/detection-power-required.md} requirement 4: state the
required sample BEFORE backtesting, not after.}

\item{ann_factor}{Numeric. Periods per year, passed to
\code{\link[=hd_detection_power]{hd_detection_power()}} when \code{expected_sharpe} is supplied. Default \code{12}.}

\item{n_tests, correction}{Passed to \code{\link[=hd_detection_power]{hd_detection_power()}}'s
multiple-testing correction. Defaults \code{1}, \code{"none"}.}

\item{target_power}{Passed to \code{\link[=hd_detection_power]{hd_detection_power()}}. Default \code{0.80}.}

\item{gate_result}{Optional tibble returned by \code{\link[=hd_strategy_value_gate]{hd_strategy_value_gate()}}.
If supplied, its \code{attr(gate_result, "overall")} is recorded and the full
tibble is serialised into \code{extra_json} via \code{\link[jsonlite:fromJSON]{jsonlite::toJSON()}}.}

\item{revises}{Logical. Must be \code{TRUE} to pre-register a strategy that
already has a SEALED admission hypothesis -- see Details. Default
\code{FALSE}.}

\item{base_dir}{Override research-log base directory (see
\code{\link[=hd_rlog_path]{hd_rlog_path()}}).}

\item{seal}{Logical. Seal the new row immediately via \code{\link[=hd_rlog_seal]{hd_rlog_seal()}}.
Default \code{TRUE}. Set \code{FALSE} only for genuinely provisional drafts that
are not yet a real pre-registration.}
}
\value{
Invisibly returns the new row's \code{uuid} (character).
}
\description{
Records a candidate strategy's admission hypothesis as a SEALED
(see \code{\link[=hd_rlog_seal]{hd_rlog_seal()}}) row in the research log's \code{hypotheses} table,
following \href{../../../.claude/rules/research-log-honesty.md}{research-log-honesty}:
sealed at inception, before any outcome is known, and never silently
overwritten. If a SEALED admission hypothesis already exists for
\code{strategy}, this function aborts unless \code{revises = TRUE}, in which case
the new row's \code{parent_uuid} links back to the prior sealed row -- a
revision is always a new, linked row, never an edit to the old one (the
research log is append-only in any case: there is no UPDATE path here).
}
\examples{
\dontrun{
hd_admission_preregister(
  strategy       = "trend_em",
  hypothesis     = "Adds trend-following diversification in EM; uncorrelated with value tilt",
  expected       = list(incr_sharpe = 0.08, var_reduction = 0.002,
                        target_regime = "trending", max_corr = 0.35),
  reviewer       = "john",
  counterparty   = "EM local funds forced to de-risk into drawdowns",
  kill_criterion = "trailing 36-month incremental Sharpe <= 0",
  expected_sharpe = 0.6
)
}
}
\seealso{
Other governance: 
\code{\link{HD_REDUNDANCY_THRESH}},
\code{\link{hd_admission_read}()},
\code{\link{hd_strategy_value_gate}()}
}
\concept{governance}
