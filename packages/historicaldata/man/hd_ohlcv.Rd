% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{hd_ohlcv}
\alias{hd_ohlcv}
\title{Query OHLCV data for one or more tickers}
\usage{
hd_ohlcv(
  ticker,
  from = NULL,
  to = NULL,
  dataset = NULL,
  local = FALSE,
  collect = TRUE
)
}
\arguments{
\item{ticker}{Ticker symbol(s). Character scalar or vector.
Single: \code{"AAPL"}. Batch: \code{c("AAPL", "MSFT", "GOOGL")}.}

\item{from}{Start date (character or Date). Default: no filter.}

\item{to}{End date (character or Date). Default: no filter.}

\item{dataset}{Dataset name from registry. If \code{NULL} (default), each ticker
is routed to its dataset via \code{\link[=detect_dataset]{detect_dataset()}} and results are bound
together. Pass an explicit dataset name to force single-dataset routing.}

\item{local}{If TRUE, query local cache instead of remote.}

\item{collect}{If TRUE, materialise immediately (backward compatible).
If FALSE (default), return a lazy duckplyr frame.}
}
\value{
Lazy duckplyr frame (collect=FALSE) or tibble (collect=TRUE)
}
\description{
Returns a duckplyr lazy frame by default. Call \code{collect()} to materialise,
or chain additional dplyr verbs for server-side computation.
}
\details{
Mixed-dataset batches (e.g. \code{c("AAPL", "BTC")}) are split by detected
dataset, queried separately, and \code{bind_rows}'d. Columns that exist in
only one dataset (e.g. \code{adjusted_close} in equities, \code{market_cap} in crypto)
are filled with \code{NA} for rows from the other dataset. When the batch
spans multiple datasets, the result is always materialised — \code{collect = FALSE} cannot be honoured because lazy frames from distinct parquet
sources cannot be bound.
}
\section{Date type guarantee (#615)}{

The \code{date} column is always returned as \code{Date}, regardless of the
underlying parquet's storage type (some sources are TIMESTAMP/POSIXct).
This holds for both \code{collect = TRUE} and \code{collect = FALSE}. Every other
exported daily-frequency accessor (\code{\link[=hd_macro]{hd_macro()}}, \code{\link[=hd_factors]{hd_factors()}}) makes
the same guarantee, so joining their outputs on \code{date} never silently
produces zero matching rows from a type mismatch.
}

\section{Point-in-time guard}{

Passing \code{to > Sys.Date()} raises a classed error (\code{"hd_future_date"}).
Future-dated requests imply look-ahead knowledge and are forbidden to
prevent silent backtest contamination.
}

\section{Non-US volume nulling (issue #21)}{

When \code{collect = TRUE}, the \code{volume} column is set to \code{NA} for tickers
on non-US exchanges (\code{.L}, \code{.DE}, \code{.PA}, \code{.AS}, \code{.SW}, \code{.MC}, \code{.MI},
\code{.ST}, \code{.CO}). yfinance reports unreliable volume for these exchanges.
The raw parquet is preserved unchanged (\code{raw-folder-readonly} rule).
When \code{collect = FALSE}, callers should apply
\code{\link[=hd_unreliable_volume_ticker]{hd_unreliable_volume_ticker()}} to filter volume themselves.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
hd_ohlcv("AAPL", from = "2024-01-01") |> collect()
hd_ohlcv(c("AAPL", "MSFT"), from = "2024-01-01", collect = TRUE)
hd_ohlcv(c("AAPL", "BTC"), from = "2024-01-01")  # mixed equity + crypto
\dontshow{\}) # examplesIf}
}
\seealso{
Other data-access: 
\code{\link{hd_alphavantage}()},
\code{\link{hd_check_accessor_date_types}()},
\code{\link{hd_check_survivorship_bias}()},
\code{\link{hd_ecb}()},
\code{\link{hd_factors}()},
\code{\link{hd_guardian}()},
\code{\link{hd_guardian_monthly}()},
\code{\link{hd_jst}()},
\code{\link{hd_kraken_ohlcvt}()},
\code{\link{hd_lazy}()},
\code{\link{hd_macro}()},
\code{\link{hd_macro_series}()},
\code{\link{hd_macro_vintages}()}
}
\concept{data-access}
