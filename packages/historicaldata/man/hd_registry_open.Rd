% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_db.R
\name{hd_registry_open}
\alias{hd_registry_open}
\title{Open the backtest registry}
\usage{
hd_registry_open(path = hd_registry_path(), read_only = TRUE)
}
\arguments{
\item{path}{Path to the registry DuckDB file. Defaults to
\code{\link[=hd_registry_path]{hd_registry_path()}}.}

\item{read_only}{Logical. Open read-only (default \code{TRUE}). Set to
\code{FALSE} when appending runs / metrics.}
}
\value{
A DBI connection object (class \code{duckdb_connection}).
}
\description{
Returns a DBI connection to the DuckDB registry file. The caller is
responsible for closing it via \code{\link[DBI:dbDisconnect]{DBI::dbDisconnect()}} (use
\code{withr::defer(DBI::dbDisconnect(con, shutdown = TRUE))} to guarantee
cleanup).
}
\examples{
\dontrun{
con <- hd_registry_open()
withr::defer(DBI::dbDisconnect(con, shutdown = TRUE))
DBI::dbGetQuery(con, "SELECT * FROM bt.strategy LIMIT 5")
}
}
