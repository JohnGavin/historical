% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cov_diagnostic.R
\name{hd_cov_oos_diagnostic}
\alias{hd_cov_oos_diagnostic}
\title{OOS Min-Variance Conditioning Diagnostic (\code{hd_cov_oos_diagnostic})}
\usage{
hd_cov_oos_diagnostic(
  returns,
  methods = c("sample", "ledoit_wolf", "rmt_denoise"),
  train_window = 60L,
  lw_target = "const_cor"
)
}
\arguments{
\item{returns}{A numeric matrix or data frame with rows in \strong{chronological
order} and one column per asset. If a data frame, a \code{date} column (any
column whose name equals \code{"date"}, case-insensitively) is dropped before
estimation and retained only for output labelling. All remaining columns
must be numeric.}

\item{methods}{Character vector of covariance methods to compare. Passed to
\code{\link[=hd_cov_estimate]{hd_cov_estimate()}}. Default \code{c("sample", "ledoit_wolf", "rmt_denoise")}.}

\item{train_window}{Integer scalar. Number of periods in each rolling
training window. Default \code{60L} (5 years of monthly data). Must satisfy
\code{nrow(returns) > train_window + 1}.}

\item{lw_target}{Ledoit-Wolf shrinkage target. Passed to \code{\link[=hd_cov_estimate]{hd_cov_estimate()}}
when \code{"ledoit_wolf"} is in \code{methods}. One of \code{"const_cor"} or
\code{"identity"}. Default \code{"const_cor"}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per method and columns:
\describe{
\item{\code{method}}{Character — covariance method name.}
\item{\code{n_oos}}{Integer — number of OOS periods with non-NA returns
(i.e. windows where the covariance solve succeeded).}
\item{\code{n_failed}}{Integer — number of windows where \code{solve()} failed
(singular or ill-conditioned covariance); results in \code{NA} OOS return.}
\item{\code{oos_mean}}{Numeric — mean of non-NA OOS returns.}
\item{\code{oos_vol}}{Numeric — standard deviation of non-NA OOS returns.}
\item{\code{oos_sharpe}}{Numeric — annualised Sharpe ratio
(\code{mean / sd * sqrt(12)}) over non-NA OOS returns. \code{NA} if fewer than
2 non-NA OOS returns.}
\item{\code{mean_cond}}{Numeric — mean condition number of the estimated
covariance across all training windows.}
\item{\code{median_cond}}{Numeric — median condition number across all
training windows.}
}
The returned tibble carries three attributes:
\describe{
\item{\code{train_window}}{The \code{train_window} argument value.}
\item{\code{n_periods}}{Total number of rows in the (date-stripped) returns
matrix.}
\item{\code{n_assets}}{Number of assets (columns).}
}
}
\description{
Evaluates multiple covariance estimators (sample, Ledoit-Wolf, RMT-denoise)
in a walk-forward out-of-sample (OOS) backtest. For each rolling training
window, each method's covariance estimate is used to compute unconstrained
global minimum-variance (GMV) portfolio weights via \code{\link[=hd_min_var_weights]{hd_min_var_weights()}}.
The resulting weights are applied to the \strong{next period's} (t+1) realised
returns to produce an OOS portfolio return.

\strong{Look-ahead discipline (mandatory):} weights are estimated exclusively
from data in the training window (periods 1 through t). The OOS return for
each origin t is the portfolio return at period t+1 — strictly the first
period \emph{not} in the training set. This mirrors the \code{alpha-decay-min-t+1}
rule: t+0 execution is impossible, and any use of t+0 information in weight
construction constitutes look-ahead bias. The function never reads
\code{returns[t+1, ]} when computing covariances.

\strong{Wide-regime demonstration:} when the number of assets p approaches or
exceeds the training window n (p ≥ train_window), the sample covariance
matrix is singular and \code{solve()} fails. These failures are counted as
\code{n_failed} in the output. Regularised estimators (Ledoit-Wolf, RMT-denoise)
remain invertible and produce finite GMV weights regardless of the p/n ratio.
}
\details{
Out-of-sample min-variance / covariance-conditioning diagnostic
}
\examples{
set.seed(42)
X <- matrix(rnorm(100 * 5), nrow = 100, ncol = 5)
colnames(X) <- paste0("A", seq_len(5))
result <- hd_cov_oos_diagnostic(X, train_window = 60L)
result
}
\references{
Raviv, E. (2026). Covariance estimation for wide data. \emph{WIREs Computational
Statistics}, 18(2). \doi{10.1002/wics.70068}

Ledoit, O. & Wolf, M. (2004). A well-conditioned estimator for
large-dimensional covariance matrices. \emph{Journal of Multivariate Analysis},
88(2), 365–411. \doi{10.1016/S0047-259X(03)00096-4}

Laloux, L., Cizeau, P., Bouchaud, J.-P. & Potters, M. (1999). Noise
dressing of financial correlation matrices. \emph{Physical Review Letters},
83(7), 1467–1470. \doi{10.1103/PhysRevLett.83.1467}
}
\seealso{
Other covariance: 
\code{\link{hd_cov_estimate}()},
\code{\link{hd_min_var_weights}()},
\code{\link{hd_min_var_weights_penalised}()}
}
\concept{covariance}
