% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_signature.R
\name{hd_roll_levy_area}
\alias{hd_roll_levy_area}
\title{Rolling Lévy area over a trailing window}
\usage{
hd_roll_levy_area(x, y, n, scale = TRUE)
}
\arguments{
\item{x, y}{Numeric vectors of equal length holding increments.}

\item{n}{Integer window length in observations. Must be at least 2.}

\item{scale}{Logical, passed to \code{\link[=hd_levy_area]{hd_levy_area()}}. Default \code{TRUE}, so
each window is normalised by its own standard deviations.}
}
\value{
Numeric vector the same length as \code{x}.
}
\description{
Length-preserving trailing-window version of \code{\link[=hd_levy_area]{hd_levy_area()}}. The first
\code{n - 1} positions are \code{NA} (warm-up), and any window containing an
\code{NA} is \code{NA} — see the missing-value note in \code{\link[=hd_levy_area]{hd_levy_area()}}.
}
\examples{
r <- stats::rnorm(100, 0, 0.01)
v <- abs(stats::rnorm(100, 0.012, 0.004))
head(hd_roll_levy_area(r, v, n = 20L), 25)

}
\seealso{
Other path-signature: 
\code{\link{hd_levy_area}()},
\code{\link{hd_path_signature2}()}
}
\concept{path-signature}
