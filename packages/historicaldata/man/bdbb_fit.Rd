% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bdbb.R
\name{bdbb_fit}
\alias{bdbb_fit}
\title{Fit rolling M/G/∞ queueing diagnostics (Varma 2026)}
\usage{
bdbb_fit(df, window_days = 30L, min_frac = 0.7)
}
\arguments{
\item{df}{Tibble with columns \code{time} (POSIXct UTC), \code{open}, \code{high}, \code{low},
\code{close}, \code{volume}, \code{trades}.  Hourly bars are assumed.}

\item{window_days}{Integer. Rolling window length in days (converted to
\code{window_days * 24L} hourly bars).  Default \code{30L}.}

\item{min_frac}{Numeric in (0, 1]. Minimum fraction of \code{n_bars} that must
have non-NA log-returns for a window to be computed.  Default \code{0.7}.}
}
\value{
A tibble with one row per complete rolling window, columns:
\code{window_end}, \code{R}, \code{theta}, \code{half_life_hours}, \code{signed_flow_mean},
\code{amihud_mean}, \code{kyle_mean}, \code{n_obs}, \code{regime}.
}
\description{
Computes rolling R-metric (variance per unit signed flow), mean-reversion
rate θ, resilience half-life, and liquidity proxies (Amihud, Kyle) for a
sequence of hourly OHLCVT bars.  Suitable for the SOL/USD Kraken dataset
returned by \code{\link[=hd_kraken_ohlcvt]{hd_kraken_ohlcvt()}}.
}
\section{Regime classification}{

\itemize{
\item \code{"mean_reversion"} — θ > 0.005 (positive ACF decay slope)
\item \code{"momentum"} — θ < -0.005 (negative decay slope)
\item \code{"ambiguous"} — insufficient lags or |θ| ≤ 0.005
}
}

\section{Look-ahead safety}{

The diagnostic at \code{window_end = t} uses only bars up to and including \code{t}.
No future data enters the window.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
sol <- hd_kraken_ohlcvt("SOL", interval_min = 60L)
bdbb_fit(sol, window_days = 30L)
\dontshow{\}) # examplesIf}
}
\seealso{
Other bdbb: 
\code{\link{bdbb_half_life}()},
\code{\link{bdbb_tail_predict}()}
}
\concept{bdbb}
