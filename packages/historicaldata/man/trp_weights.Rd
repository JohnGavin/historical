% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topological_risk_parity.R
\name{trp_weights}
\alias{trp_weights}
\title{Topological Risk Parity (TRP) portfolio weights}
\usage{
trp_weights(cov_mat, fallback_to_hrp = TRUE)
}
\arguments{
\item{cov_mat}{Numeric matrix. Named, symmetric, positive-semi-definite
covariance matrix (n x n). Row and column names must match.}

\item{fallback_to_hrp}{Logical. If \code{TRUE} (default), fall back to
\code{hrp_weights()} if the MST ordering step fails. If \code{FALSE}, error on
failure.}
}
\value{
Named numeric vector of portfolio weights summing to 1. All weights
are strictly positive.
}
\description{
Extends HRP with a Minimum Spanning Tree (MST) step that groups assets by
network topology before hierarchical clustering. The MST is constructed from
the same correlation-distance matrix used by HRP. Assets connected in the
same MST component receive cluster membership, which biases the quasi-
diagonal ordering to respect topological proximity.
}
\details{
\strong{Algorithm:}
\enumerate{
\item Build correlation-distance matrix from \code{cov_mat}.
\item Extract the MST (Prim's algorithm, base R, no external graph package).
\item Derive MST-based asset ordering from a depth-first traversal of the tree.
\item Apply recursive bisection (as in HRP) using this ordering.
}

\strong{Deferred:} Full TDA (persistent homology / Betti numbers) extension from
VertoxQuant (2024). Requires the TDA/TDAstats package and is out of scope
for the #114 first cut. Flagged for a follow-up issue.
}
\examples{
set.seed(42)
n <- 5
R <- matrix(c(1,.8,.1,.1,.0,
              .8,1,.1,.1,.0,
              .1,.1,1,.7,.2,
              .1,.1,.7,1,.2,
              .0,.0,.2,.2,1), nrow = n)
sds <- c(.02,.03,.015,.025,.02)
cov_mat <- diag(sds) \%*\% R \%*\% diag(sds)
rownames(cov_mat) <- colnames(cov_mat) <- paste0("A", seq_len(n))
w <- trp_weights(cov_mat)
stopifnot(abs(sum(w) - 1) < 1e-10, all(w > 0))
}
\references{
VertoxQuant (2024). \emph{Topological Risk Parity}.
\url{https://www.vertoxquant.com/p/topological-risk-parity}

Lopez de Prado, M. (2016). Building diversified portfolios that outperform
out-of-sample. \emph{Journal of Portfolio Management}, 42(4), 59–69.
}
\seealso{
Other topological_risk_parity: 
\code{\link{hrp_weights}()}
}
\concept{topological_risk_parity}
