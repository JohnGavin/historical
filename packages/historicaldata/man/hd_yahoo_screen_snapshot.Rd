% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yahoo_screen.R
\name{hd_yahoo_screen_snapshot}
\alias{hd_yahoo_screen_snapshot}
\title{Snapshot a Yahoo Finance screener result to parquet}
\usage{
hd_yahoo_screen_snapshot(
  region,
  sec_type = c("etf", "equity", "mutualfund", "index", "future"),
  out_dir,
  date = Sys.Date(),
  min_rows = 10L,
  max_total = 10000L
)
}
\arguments{
\item{region}{Two-letter Yahoo region code (e.g. \code{"gb"}, \code{"us"}, \code{"de"}).}

\item{sec_type}{One of \code{"etf"}, \code{"equity"}, \code{"mutualfund"}, \code{"index"}, \code{"future"}.}

\item{out_dir}{Directory for the parquet. Created if missing.}

\item{date}{Snapshot date (defaults to today); embedded in the filename.}

\item{min_rows}{Soft floor — warn if the screener returned fewer than this.}

\item{max_total}{Upper bound on rows to retrieve. yfscreen's \code{size} argument
is the TOTAL desired (not page size); the function chunks internally at
250/request and stops early when a page returns empty. Default 10000 is
enough to capture any single-region universe; lower it for quick smoke
tests.}
}
\value{
Path to the written parquet (invisibly).
}
\description{
Wraps the \link[yfscreen:yfscreen-package]{yfscreen} API to capture a one-shot
universe snapshot for a \verb{(region, sec_type)} pair (e.g. UK-listed ETFs).
Intended for periodic offline ingestion, not for live querying.
}
\details{
yfscreen depends on undocumented Yahoo internals (crumb-cookie auth) and has
been broken upstream more than once. Schema-stability is checked at fetch
time and a warning is emitted if expected columns are missing.
}
\seealso{
Other discovery: 
\code{\link{hd_datasets}()},
\code{\link{hd_ecb_registry}()},
\code{\link{hd_exchanges}()},
\code{\link{hd_fred_meta}()},
\code{\link{hd_jst_variables}()},
\code{\link{hd_macro_forward}()},
\code{\link{hd_macro_registry}()},
\code{\link{hd_macro_series}()},
\code{\link{hd_search}()},
\code{\link{hd_summary}()},
\code{\link{hd_ticker_info}()},
\code{\link{hd_ticker_meta}()},
\code{\link{hd_tickers}()}
}
\concept{discovery}
