% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_contribution.R
\name{hd_risk_contribution}
\alias{hd_risk_contribution}
\title{Portfolio Risk Contribution (\code{hd_risk_contribution})}
\usage{
hd_risk_contribution(w, cov_mat)
}
\arguments{
\item{w}{A numeric vector of portfolio weights, length \code{p}. May contain
negative values (short positions). May be named.}

\item{cov_mat}{A numeric \verb{p x p} covariance matrix. Must be square,
symmetric (within floating-point tolerance), and have \code{nrow(cov_mat)}
equal to \code{length(w)}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per asset and columns:
\describe{
\item{\code{asset}}{Character. Taken from \code{names(w)} if present, else from
\code{colnames(cov_mat)}, else \code{"asset_1"}, \code{"asset_2"}, ....}
\item{\code{weight}}{Numeric. The input weight \code{w_i}.}
\item{\code{mcr}}{Numeric. Marginal contribution to risk,
\code{(Sigma \%*\% w)[i] / sigma_p}.}
\item{\code{cr}}{Numeric. Component contribution to risk,
\code{w_i * mcr_i}. \code{sum(cr)} equals portfolio volatility \code{sigma_p}
(the Euler identity).}
\item{\code{pct_contribution}}{Numeric. \code{cr_i / sigma_p}, the fraction of
total portfolio risk attributable to asset \code{i}. Sums to 1 across
assets. May be negative for a hedging short position.}
}
When \code{sigma_p} is numerically zero (e.g. an all-zero \code{w}, or a
perfectly hedged degenerate case), \code{mcr}, \code{cr}, and \code{pct_contribution}
are \code{NA_real_} for every asset rather than a division-by-zero result.
}
\description{
Decomposes total portfolio volatility into each asset's marginal and
component contribution using the standard Euler decomposition:

\deqn{\sigma_p = \sqrt{w^\top \Sigma w}}
\deqn{MCR_i = \frac{(\Sigma w)_i}{\sigma_p}}
\deqn{CR_i = w_i \cdot MCR_i}

By Euler's theorem for the (positive-homogeneous-of-degree-1) portfolio
volatility function, the component contributions sum exactly to total
portfolio volatility: \eqn{\sum_i CR_i = \sigma_p}. This identity is the
correctness anchor for this function and is asserted directly in its
test suite.

\strong{Why this differs from weight concentration:} \code{mean_eff_n} and
\code{max_abs_weight} (\code{\link[=hd_weight_stability_diagnostic]{hd_weight_stability_diagnostic()}}) summarise the
\emph{weight} vector alone and treat every asset as equally risky and
uncorrelated. A 5\%-weight position in a high-volatility asset, or a
cluster of mutually correlated positions, can dominate portfolio variance
while appearing well diversified on weights alone. \code{hd_risk_contribution()}
answers "where does the portfolio's \emph{risk} actually come from," using the
full covariance structure rather than weights in isolation.

\strong{Negative weights and negative contributions are both intentional.}
This project routinely runs long/short books. A short position
(\code{w_i < 0}) can have a \emph{negative} \code{pct_contribution} — meaning it reduces
rather than adds to total portfolio risk (a hedge). A negative
\code{pct_contribution} is a correct hedging signal, not a bug or a sign that
the computation has failed.

\strong{Rank-deficient covariance is supported.} Estimated covariance matrices
in this project are routinely rank-deficient or ill-conditioned (see
\code{\link[=hd_cov_estimate]{hd_cov_estimate()}} and its regularisation methods). The Euler
decomposition above is well-defined for any positive semi-definite \code{Sigma}
— this function does \strong{not} require or check positive-definiteness.
}
\details{
Component and marginal contribution to portfolio risk (Euler decomposition)
}
\examples{
# Three-asset example: equal weights, unequal variances + correlation
Sigma <- matrix(
  c(0.04, 0.01, 0.00,
    0.01, 0.09, 0.02,
    0.00, 0.02, 0.01),
  nrow = 3, dimnames = list(c("A", "B", "C"), c("A", "B", "C"))
)
w <- c(A = 1 / 3, B = 1 / 3, C = 1 / 3)
hd_risk_contribution(w, Sigma)

# Long/short: the short leg (D) can show a negative pct_contribution,
# meaning it hedges rather than adds to total portfolio risk.
Sigma2 <- matrix(
  c(0.04, 0.03,
    0.03, 0.04),
  nrow = 2, dimnames = list(c("C_long", "D_short"), c("C_long", "D_short"))
)
hd_risk_contribution(c(C_long = 1, D_short = -0.5), Sigma2)

}
\seealso{
Other risk_metrics: 
\code{\link{hd_dd_duration}()},
\code{\link{hd_exposure_metrics}()},
\code{\link{hd_loss_clustering}()}
}
\concept{risk_metrics}
