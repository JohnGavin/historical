% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/admission_preregister.R
\name{hd_admission_read}
\alias{hd_admission_read}
\title{Read strategy-admission pre-registrations from the research log}
\usage{
hd_admission_read(strategy = NULL, base_dir = NULL)
}
\arguments{
\item{strategy}{Optional character scalar. If supplied, returns only rows
for this strategy.}

\item{base_dir}{Override research-log base directory (see
\code{\link[=hd_rlog_path]{hd_rlog_path()}}).}
}
\value{
A tibble, one row per pre-registration, oldest first. Zero rows
(with the correct columns) if no admission hypotheses have been
recorded yet.
}
\description{
Returns every \code{hypotheses} row written by \code{\link[=hd_admission_preregister]{hd_admission_preregister()}}
(identified by \code{extra_json$kind == "strategy_admission"}), with the
admission-specific fields unpacked into columns. A strategy that has been
revised (see \code{\link[=hd_admission_preregister]{hd_admission_preregister()}}'s \code{revises} argument) appears
as multiple rows linked via \code{parent_uuid} -- this function returns ALL of
them (oldest first); pass \code{strategy} to filter, or take the last row per
\code{strategy} for "current" expectations.
}
\examples{
\dontrun{
hd_admission_read()
hd_admission_read(strategy = "trend_em")
}
}
\seealso{
Other governance: 
\code{\link{HD_REDUNDANCY_THRESH}},
\code{\link{hd_admission_preregister}()},
\code{\link{hd_strategy_value_gate}()}
}
\concept{governance}
