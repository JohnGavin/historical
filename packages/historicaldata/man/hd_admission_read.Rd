% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_admission.R
\name{hd_admission_read}
\alias{hd_admission_read}
\title{Read the strategy admission table}
\usage{
hd_admission_read(con = NULL, path = hd_registry_path())
}
\arguments{
\item{con}{Optional DBI connection. If \code{NULL} (default), opens a read-only
connection to \code{path} and closes it on exit.}

\item{path}{Path to the registry DuckDB file. Defaults to
\code{\link[=hd_registry_path]{hd_registry_path()}}. Ignored when \code{con} is supplied.}
}
\value{
A tibble with one row per registered strategy and columns as defined
in \code{\link[=hd_admission_init]{hd_admission_init()}}. Returns a zero-row tibble if the table has never
been initialised.
}
\description{
Returns the full \code{strategy_admission} table as a tibble. Opens a read-only
connection if \code{con} is not supplied.
}
\examples{
\dontrun{
# Read against the default registry
hd_admission_read()
}
}
\seealso{
Other governance: 
\code{\link{hd_admission_init}()},
\code{\link{hd_admission_register}()},
\code{\link{hd_strategy_value_gate}()}
}
\concept{governance}
