# function signatures are stable (catches API drift)

    Code
      args(hd_caption)
    Output
      function (text, short = NULL, threshold = 300, summary_label = "Show full details", 
          style_extra = "") 
      NULL

---

    Code
      args(hd_link)
    Output
      function (label, href) 
      NULL

