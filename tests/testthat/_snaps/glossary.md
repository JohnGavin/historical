# default path aborts loudly, listing searched dirs, when no data/ ancestor exists

    Code
      cat(strsplit(msg, "\n")[[1]][1])
    Output
      x File 'data/glossary.yaml' not found in the working directory or any ancestor.

